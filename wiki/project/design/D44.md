# Decision 44 — The ask result page (`GET /{$}` with `?q=…`) + the `MentionsIn` seam

**Decision.** When the root route (D42) sees a non-empty `q`, it calls `Asker.Ask(ctx, owner, q)` (D9) and renders `answer.tmpl`. `owner` is the nginx-supplied `X-Owner-Email` header (the session-gated request carries it, D47); `ask` is owner-scoped only for call recording — the answer content is the same read-only synthesis D9 already produces. The web adds **no** answering logic; it renders D9's `Answer` and decorates it with a mention footer.

**Layout, top to bottom (`answer.tmpl`):**
- **"Ask another question"** — a link to `.` (resolves to `<base>`, the reset home D43). Present on every answer page, `Found` or not.
- **Answer prose** — `Answer.Text` is first passed through `Service.LinkifyMentions(ctx, text, webBase, "")` (D58 — the first occurrence of each named subject wrapped as an inline link to its absolute front-door URL; no own-subject on ask, so `excludeID == ""`), then rendered as **sanitized markdown HTML** via the D48 `md` template func (`{{ md .Answer.Text }}` over the linkified text), not an escaped paragraph, so a synthesized answer reads like an article (headings, emphasis, lists, tables) with its named subjects linked inline at first mention. goldmark renders the injected `[…](https://…)` as `<a>` and bluemonday passes the absolute https href. On `Found:true` this is the cited synthesis; on `Found:false` it is D9's fixed honest-empty sentence (plain prose, which names no subject, so linkify is a no-op and `md` round-trips it to a `<p>`). **Both render identically** — the honest-empty text is just the body; there is no separate "no results" branch or styling. The injection guard is D48's sanitizer (raw HTML/`javascript:` neutralized), not template escaping.
- **Outbound footer** — the distinct subjects whose name or alias occurs in the answer prose, each a subject link `<a href="{{.Href}}">{{.Name}}</a>` whose `Href` is the **absolute front-door URL** `webBase + Path` (D59). Omitted whole when empty (e.g. honest-empty, whose sentence names no subject). Outbound only — an answer is ephemeral, nothing points *to* it, so there is no inbound section (product decision). A subject named in the answer thus appears both inline (at first mention) and in this footer, pointing at the identical absolute URL.

## The `MentionsIn` seam

The outbound footer is **every subject/alias occurrence in the prose** (the product call), not the citations — so it is `Mentions(answerText, allSubjectKeys)`, the pure D12 matcher. D12 loads `SubjectKeys` (subjects ∪ D25 aliases) only inside `PageWithLinks`; this Decision exposes that over arbitrary text as a new method on `wiki.Service`, reusing the exact D12 matcher with no new matching logic:

```go
// MentionsIn loads the current SubjectKeys (all subjects ∪ their D25 alias keys)
// and returns every subject whose name or an alias key occurs as a whole,
// hyphen/edge-bounded run (D12 Mentions) in text — rendered as web Refs
// (Href = "subject/<type>/<slug>", Name = canonical display name). A match via an
// alias key resolves to the CANONICAL subject (D12), so a folded short name in the
// answer links to the survivor. Read-only; no write path.
func (s *Service) MentionsIn(ctx context.Context, text string) ([]Ref, error)
```

The web handler's ask path injects this as a second capability beside `Asker` (a `Mentioner` seam, `MentionsIn(ctx, text) ([]Ref, error)`), plus a `Linkifier` seam (`LinkifyMentions`, D58) for the inline prose links, and the `cmd/wiki/main.go` adapter maps `wiki.Ref{Path,Name}` → web `Ref{Href: webBase + Path, Name}` — the **absolute** front-door URL (D59), not the former relative `"subject/"+Path`. Targets are de-duplicated and deterministically ordered (by path), matching D12's footer discipline.

**Why not the citations.** `Answer.Citations` (D9) are only the pages the synthesis drew on; the product requires linking *every* subject/alias the prose names, cited or not. `MentionsIn(answerText)` is that set and reuses D12 exactly, so the answer footer and a page footer share one matcher and one alias semantics.

**Read-only.** `Ask`, `MentionsIn`, and `LinkifyMentions` (D58) only read; the ask page mutates nothing (D9 `R-5X56-NF4O` holds; neither `MentionsIn` nor `LinkifyMentions` has a write path).

**Rejected.**
- **Footer from `Answer.Citations`.** Links only cited subjects, contradicting the product's "every occurrence of any subject/alias." Rejected.
- **A distinct honest-empty page/branch.** D9 already returns a usable `Answer.Text` for the empty case; rendering it as the ordinary body keeps one template and one code path, and the omitted-when-empty footer falls out naturally. Rejected.
- **Re-implementing mention detection in the web layer.** Would fork D12's alias-aware matcher and risk drift. `MentionsIn` reuses it. Rejected.
- **Threading `owner` into the answer rendering.** The answer is identity-independent content; `owner` is only `ask`'s call-recording scope. The template reads no identity. Rejected.

**Verification.** Page-shape ids: httptest against the D42 handler with a stub `Asker` and stub `Mentioner` — no DB/LLM. The `MentionsIn` matcher and the end-to-end wiring get real substrates as noted.

- R-ARN9-5YPS — **Renders the synthesized answer.** `GET /?q=who%20is%20acme` with a stub `Asker` returning `Answer{Found:true, Text:"Acme Corp makes widgets.", Citations:…}` produces a 200 `text/html` page whose body contains `Acme Corp makes widgets.` — the page renders `Answer.Text`. *(httptest, stub Asker)*
- R-ASV5-JQGH — **"Ask another question" returns to home.** Every answer page (both `Found:true` and `Found:false`) contains an "ask another question" control whose href resolves to the base (`.` / the mount root), so it navigates to the reset home (D43). *(httptest, stub Asker)*
- R-AU31-XI76 — **Outbound footer links every mentioned subject, absolute URL.** With a stub `Mentioner` returning two Refs whose `Href` is the absolute form (`{Href:"https://acct.ikigenba.com/srv/wiki/subject/entity/acme-corp",Name:"Acme Corp"}`, `{Href:"https://acct.ikigenba.com/srv/wiki/subject/concept/widget",Name:"Widget"}`), the answer page renders both as `<a href="https://acct.ikigenba.com/srv/wiki/subject/entity/acme-corp">Acme Corp</a>` / `<a href="…/subject/concept/widget">Widget</a>` — proving the page calls the mentions seam over the answer text and emits each as a fully-qualified subject link (D59). *(httptest, stub Mentioner)*
- R-AVAY-B9XV — **Empty mentions omit the footer.** With a `Mentioner` returning an empty slice, the answer page renders the answer prose and the "ask another question" control but **no** outbound footer markup (no `---`/section/`<ul>`). *(httptest, stub Mentioner)*
- R-AWIU-P1OK — **Honest-empty renders as an ordinary usable page.** `GET /?q=…` with a stub `Asker` returning `Answer{Found:false, Text:<honest-empty sentence>, Citations:nil}` produces a 200 page whose body contains that sentence and the "ask another question" control, with no outbound footer — no error, no separate branch. *(httptest, stub Asker)*
- R-NPVU-26CX — **Answer prose is rendered as markdown, not escaped raw text.** `GET /?q=…` with a stub `Asker` returning `Answer{Found:true, Text:"**Acme** makes widgets."}` produces a page whose body contains `<strong>Acme</strong>` — proving the answer text is passed through the D48 `md` renderer (a wrong impl emitting the literal `**Acme**` or `&#42;&#42;` fails this). *(httptest, stub Asker; composes with D48)*
- R-AXQR-2TF9 — **End-to-end over the real pipeline (not only stubs).** Driving the web handler wired to the **real** `asker.Ask` + a **real temp SQLite** (migrated, holding a subject `entity/acme-corp` with a page) + the **mock LLM provider** (scripted to synthesize an answer naming "Acme Corp" with a citation) and the **real `MentionsIn`**: `GET /?q=…` returns a 200 page containing the synthesized answer text **and** an outbound footer linking the absolute `…/srv/wiki/subject/entity/acme-corp` (D59) — proving the composition-root adapters actually connect `Asker` and `Mentioner` to the web handler over real data, a wiring no stub test exercises. *(integration: real DB + real asker/MentionsIn + mock provider; the LLM stays mocked per the Testing strategy — no live call)*
- R-8FQU-M1J4 — **Answer prose carries inline first-occurrence links.** Driving the web handler wired to the **real** `LinkifyMentions` over a **real temp SQLite** holding subject `entity/acme-corp` (with a page) + a stub/real `Asker` returning `Answer{Found:true, Text:"Acme Corp makes widgets."}`: `GET /?q=…` produces a 200 page whose **prose** contains `<a href="https://…/srv/wiki/subject/entity/acme-corp">Acme Corp</a>` at the first mention (rendered from the injected markdown link through the D48 `md` func), proving the ask path routes `Answer.Text` through the D58 linkifier before rendering — a wrong impl that renders the raw text with no inline link fails. *(integration: real DB + real LinkifyMentions through the web handler; composes with D58/D48/D59)*
