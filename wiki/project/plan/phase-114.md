# Phase 114 — Tune folders: extract and analysis, with their deterministic scorers

*Realizes design Decision 71 (workspace, extract/analysis slice) and 72 (extract + analysis scorer ids). Depends on Phase 112 and 113 (the in-code prompts the folders copy).*

Create `autotune/extract/` and `autotune/analysis/` per the D71 contract. Extract: copy the cases (15 dev / 8 holdout) and `score` **verbatim** from `~/projects/autotune/examples/extract/cases` and its scorer; `prompt.txt` is a copy of `internal/extract/prompt.txt` (not the example's naive prompt); write `improve.md`, `config.json` (D71 pins), per-folder `README.md` and `.gitignore`, and `fixtures/` with hand-computed expected scores. Analysis: convert the existing analysis gold (the 14 dev / 7 holdout question cases under `eval/analysis/gold/`, including the as-yet-uncommitted case directories in the working tree) into `cases/` (`input.txt` = the production-rendered user message, i.e. the question; `gold.json` = the gold lists), port the retired Go scorer's three-list math to a python3 `score` (embeddings via the `embed` CLI), `prompt.txt` copied from `internal/ask/analysis-prompt.txt`, plus improve.md/config/README/.gitignore/fixtures. Add `autotune/folders_test.go` (test-only package) shelling both scorers over their fixtures; folder-structure assertions extend in Phases 116/117 (D71's structure ids land in 117 when all four folders exist). Remove the old `/autotune/` line's effect by keeping the new tree committed (the `.gitignore` root change itself belongs to Phase 115; until then `git add -f` is acceptable for the new tree, or reorder the two-line ignore edit into this phase — either way the new folders end up tracked).

**Done when:**
- R-AAOL-YG21 — the extract scorer reproduces its fixture's expected score and floors a malformed candidate output.
- R-ABWI-C7SQ — the analysis scorer reproduces the retired Go math's expected three list scores and composite on its fixture.
- `autotune/extract` and `autotune/analysis` each contain `prompt.txt`, `improve.md`, executable `score`, `config.json`, `.gitignore`, non-empty `cases/dev` and `cases/holdout`, and `fixtures/` — verified by the new test package.
- The suite is green (design Conventions).
