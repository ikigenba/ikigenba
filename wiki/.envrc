# Inherit the repo-root .envrc (GOFLAGS=-buildvcs=false for the worktree layout).
source_up

if [ -f "$HOME/.secrets/ANTHROPIC_API_KEY" ]; then
	export ANTHROPIC_API_KEY="$(cat "$HOME/.secrets/ANTHROPIC_API_KEY")"
fi
