{{define "home"}}{{template "head" .}}
<h1>{{.Service}}</h1>
<form action="" method="get" role="search">
  <input type="text" name="q" aria-label="Search wiki" value="{{.Query}}">
  <button type="submit">Search</button>
</form>
{{if .Asked}}
<article aria-label="Answer">
  <h2>{{.Query}}</h2>
  <div class="prose">{{.AnswerHTML}}</div>
  {{if .Cites}}
  <nav aria-label="Cited pages">
    <ul>
      {{range .Cites}}<li><a href="{{.Href}}">{{.Name}}</a></li>{{end}}
    </ul>
  </nav>
  {{end}}
  {{if .Mentions}}
  <nav aria-label="Mentioned subjects">
    <ul>
      {{range .Mentions}}<li><a href="{{.Href}}">{{.Name}}</a></li>{{end}}
    </ul>
  </nav>
  {{end}}
</article>
{{else}}
{{if .Searched}}
{{if .Results}}
<nav aria-label="Search results">
  <ul>
    {{range .Results}}<li><a href="{{.Href}}">{{.Name}}</a></li>{{end}}
  </ul>
</nav>
{{else}}
<p>No matches.</p>
{{end}}
{{else}}
{{if .Browse}}
<nav aria-label="Browse subjects">
  <ul>
    {{range .Browse}}<li><a href="{{.Href}}">{{.Name}}</a></li>{{end}}
  </ul>
</nav>
{{end}}
{{if .Orphans}}
<nav aria-label="Orphan pages">
  <ul>
    {{range .Orphans}}<li><a href="{{.Href}}">{{.Name}}</a></li>{{end}}
  </ul>
</nav>
{{end}}
{{end}}
{{end}}
{{template "foot" .}}{{end}}
