logs/
tmp/
nginx.pid
# Dev /srv/<svc>/ location fragments generated by ./run from each service's
# templated etc/nginx.conf — not committed (the .gitkeep keeps the dir).
locations/*.conf

# Build-loop ephemeral seam (see project/loops/README.md) — never committed.
project/loops/brief.md
project/loops/blocked.md
