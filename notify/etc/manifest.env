APP=notify
MOUNT=/srv/notify/
DEFAULT=false
PORT=3003
# MCP=true marks this service for inclusion in the dashboard service inventory
MCP=true
# Comma-separated upstream producer names whose feeds this service consumes.
CONSUMES=crm
