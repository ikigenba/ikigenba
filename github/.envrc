# Inherit the repo-root .envrc (GOFLAGS=-buildvcs=false for the worktree layout).
source_up

export IKIGENBA_APP_ID="$(cat ~/.secrets/IKIGENBA_APP_ID)"
export IKIGENBA_APP_PRIVATE_KEY="$(cat ~/.secrets/IKIGENBA_APP_PRIVATE_KEY)"
export IKIGENBA_GITHUB_ORG="$(cat ~/.secrets/IKIGENBA_GITHUB_ORG)"
