**/.terraform/*
# Bootstrap state is local and committed: it is the only record that
# terraform owns the state buckets. Everything else is in S3.
!bootstrap/295229566359/terraform.tfstate
!bootstrap/295229566359/terraform.tfstate.backup
!bootstrap/602773793009/terraform.tfstate
!bootstrap/602773793009/terraform.tfstate.backup
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
