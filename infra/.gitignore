**/.terraform/*
# Bootstrap state is local and committed: it is the only record that
# terraform owns the state buckets. Everything else is in S3. The .backup
# snapshots churn on every apply and are not committed.
*.tfstate.backup
!bootstrap/295229566359/terraform.tfstate
!bootstrap/602773793009/terraform.tfstate
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
