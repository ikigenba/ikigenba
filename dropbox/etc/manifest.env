APP=dropbox
MOUNT=/srv/dropbox/
DEFAULT=false
PORT=3005
# MCP=true marks this service for inclusion in the dashboard service inventory
MCP=true
# Presence of FEED marks this service as an event-plane producer and names its
# outbox endpoint (consumers resolve it via `bin/registry feed-url dropbox`).
FEED=/feed
