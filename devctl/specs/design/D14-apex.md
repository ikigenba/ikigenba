# D14-apex

The root domain is not a space. It is one `A` record in the zone, at the root
itself, pointing at the Elastic IP of one space, and on that space one app
answers at it. `apex` is the command that makes, reads, and unmakes that choice
from the developer's machine, and `internal/apex` owns it. The record is `A`
only: `*.<root>` is never written, so nothing under the root resolves unless a
space owns it.

The vocabulary is the platform's. The root and its region come from the
checkout's root file (D04). The holder is the space whose Elastic IP the root's
`A` record points at, found the way `space list` and `space destroy` find it,
by `space.FindApex` (D06): the record, then the tagged addresses, then the
`Space` tag. No host is asked to learn the holder; a host is asked only which
app answers there, which is the opsctl configuration key `host.apex`. That
key, the certificate that carries the root as a third name, and the nginx
route from the root to the app are opsctl's, reached through its published
interface over ssh: the configuration store through `hostsetup`'s single-key
operations (D10), and `cert obtain` and `nginx apply` issued directly through
`(host.Host).Sudo` as every other host command is.

`set` takes the one app hostname devctl accepts, `<app>.<space>`, parsed by
`spaceref.ParseApp` and by nothing else; the reserved app names and the
refusal texts are D04's. Its preflight refuses before anything is printed or
changed: the target must exist and be running; the record, if there is one,
must point at a space's address; and the current holder, when it is another
space, must be running too, because its host is about to give the apex up.
Then five steps, in an order that never lets the root resolve to a host that
would refuse the handshake. `space` reports the target. `role` regenerates the
target's inline policy with the `_acme-challenge.<root>` permission, using
`space.PolicyDocument`, the builder `space create` uses (D06), so the two can
never disagree about what a space's role may do. `host` sets `host.apex`, reissues
the certificate, and applies nginx. `record` upserts the root's `A` record to
the target's address and waits for `INSYNC`. `previous` clears the old holder
in the reverse order, its policy narrowed first and then its host, or reports
`none`. Every step runs on every `set`, whether or not the target already
holds the apex, so a retry repeats from the start and a `set` that changes
nothing has the same shape as one that changes everything.

A failure stops the command at that step: the completed lines stay on stdout,
the failed step's error is the diagnostic, and nothing is undone. A cloud
failure is the `*cloud.Error` or `*space.WaitError` returned unchanged, as it
is in `space destroy`; a host command that exits non-zero is the
`*host.CommandError` that already carries the step name; and a host the probe
cannot reach is the `*host.UnreachableError`, wrapped so that the step name
leads the line, which is the one error shape this package adds for output.
The three refusals whose texts the stories fix and no other design declares —
a holder that is stopped while another space is about to take the apex, a
record that points at no space's address, and a holder whose host has no
`host.apex` — are this package's error types.

`show` prints the app's hostname and the address, one line, or nothing at all
when there is no record. `clear` is `set`'s reverse in the reverse order,
record, then role, then host, and needs no host when there is no record or
when the record points at no space's address. Both refuse a stopped holder. A
record that points at no space's address was not devctl's doing and is
reported, not guessed at; a holder whose host has no `host.apex` is the same
kind of inconsistency and `show` reports it the same way.

Ordering and the cli mapping are the shared rules and are not restated here:
usage errors first, then the root file, then the operand, then the first cloud
call (D04 R-N1LD-IX1I and R-QW0J-KUFF); `cloud.Connect` with the root as the
profile and the root file's region (D02 R-OO0I-HZUA); a `*cloud.NoSpaceError`
to `devctl: no space at '<domain>'`, exit 1 (D03 R-R10I-DEAA); a `*cloud.Error`
or `*cloud.NotFoundError` to one line and exit 1 (D03 R-QZSL-ZMJL); the
checkout and root-file errors to exit 2 (D04 R-QEXY-821P); every error carrying
`ExitCode()` and `Detail()` (D05 R-D4G2-IO81); the fallback that turns a
`*space.NotRunningError` into `devctl: '<domain>' is stopped`, exit 1 (D05
R-0D99-FN33); and every step line written through `space.Step` (D06
R-VSDZ-20UO). The host runner is D06's `internal/host`: `host.Host{Address,
Deps}`, `Wait`, `Sudo`, `*host.CommandError`, and `*host.UnreachableError`.

## REQUIREMENTS

- R-Q4D0-4OUV: Package `internal/apex` MUST export `Run(ctx context.Context, args []string, stdout io.Writer, deps seam.Deps) error`, and `Run` MUST take no writer other than `stdout`.

- R-4282-JQL9: Package `internal/apex` MUST export a `HolderStoppedError` struct whose fields are exactly `Domain string`, `Label string`, and `State cloud.InstanceState`, with the methods `Error() string`, returning `'<Domain>' holds the apex and is <State>`, `ExitCode() int`, returning 1, and `Detail() string`, returning `run 'devctl space start <Label>' first`, verified at least by reproducing `'sbx2.ikigenba.dev' holds the apex and is stopped` and `run 'devctl space start sbx2' first`.

- R-43FY-XIBY: Package `internal/apex` MUST export a `NotSpaceAddressError` struct whose fields are exactly `Root string` and `Values []string`, whose `Error()` returns `<Root> points at <Values joined by ", ">, which is not a space's address`, verified at least by reproducing `ikigenba.dev points at 203.0.113.9, which is not a space's address` for the single value `203.0.113.9`.

- R-44NV-BA2N: Package `internal/apex` MUST export a `NoApexAppError` struct whose fields are exactly `Root string` and `Domain string`, whose `Error()` returns `<Root> points at <Domain>, whose host.apex is not set`, verified at least by reproducing `ikigenba.dev points at sbx1.ikigenba.dev, whose host.apex is not set`.

- R-45VR-P1TC: Package `internal/apex` MUST export a `StepError` struct whose fields are exactly `Step string` and `Err error`, with the methods `Error() string`, returning `<Step>: ` followed by the message of `Err`, `Unwrap() error`, returning `Err`, `ExitCode() int`, returning 1, and `Detail() string`, returning what `Err`'s own `Detail() string` returns when `errors.As` matches `Err` to `interface{ Detail() string }` and an empty string otherwise, verified at least by reproducing `previous: ssh ec2-user@18.220.10.5: connection timed out` with an empty `Detail()` for a `*host.UnreachableError` whose `Address` is `18.220.10.5`.

- R-VVU4-SN6J: Package `internal/apex` MUST import, of this module, no package other than `internal/seam`, `internal/cloud`, `internal/checkout`, `internal/spaceref`, `internal/space`, `internal/host`, and `internal/hostsetup`, and MUST be imported by no package of this module other than `internal/cli`, verified by a test over the packages' import lists.

- R-48BK-GLAQ: `cli.Run` MUST dispatch the command `apex` to `apex.Run`, passing the arguments that follow `apex`, the `stdout` writer `cli.Run` was given, and `deps`, and MUST return 0 when `apex.Run` returns a nil error.

- R-49JG-UD1F: `devctl apex --help` and `devctl apex -h` MUST write exactly the following text with a final newline to stdout, with empty stderr and exit 0, whenever `--help` or `-h` appears anywhere among the arguments of `apex`, including after `set`, `show`, or `clear`; subject to the superuser refusal, help MUST call `deps.Cloud` not at all and pass no `seam.Cmd` to `deps.Exec` or `deps.Stream`, so that it neither finds a checkout nor reads the root file, verified at least by `devctl apex --help`, `devctl apex set --help`, and `devctl apex show -h` each printing that text with `Deps.Dir` set to a directory that is not inside a git checkout:

  ```
  Usage: devctl apex <subcommand> [arguments]

  Point the root domain at one app on one space, say where it points, or take
  it away. The root is an A record at the space's address; the space's host
  carries the root in its certificate and routes it to the app.

  Subcommands:
    set <app>.<space>   make <app> on <space> answer at the root
    show                print the app and address the root points at
    clear               remove the root's record and the holder's apex configuration

  Run 'devctl apex <subcommand> --help' for details.
  ```

- R-4ARD-84S4: The `apex` subcommands MUST be exactly `set`, `show`, and `clear`; `set` MUST take exactly one operand, `<app>.<space>`, and `show` and `clear` MUST take none; and none of the three, nor `apex` itself, MUST accept any option other than `--help` and `-h`.

- R-4BZ9-LWIT: `apex` argument failures MUST return a `*space.UsageError` whose `Help` is `devctl apex --help` and whose `Message` is `apex needs <subcommand>` when no argument follows `apex`, `unknown subcommand '<name>'` when the first argument is none of the subcommands and does not begin with `-`, `apex set needs <app>.<space>` when `set` has no operand, `apex set takes only <app>.<space>` when it has more than one, `apex show takes no arguments` when `show` has an operand, `apex clear takes no arguments` when `clear` has one, and `unknown option '<option>'` for an argument that begins with `-` and is neither `--help` nor `-h`; on these failures `Run` MUST call neither `checkout.ReadRootFile` nor `checkout.Open`, MUST pass no `seam.Cmd` to `deps.Exec` or `deps.Stream`, and MUST call `deps.Cloud` not at all; verified at least through `cli.Run`, with `Deps.Dir` set to a directory that is not inside a git checkout, by `devctl apex set` writing exactly the three lines `devctl: apex set needs <app>.<space>`, an empty line, and `see 'devctl apex --help' for usage` to stderr, by `devctl apex show sbx1` writing exactly the three lines `devctl: apex show takes no arguments`, an empty line, and `see 'devctl apex --help' for usage`, and by `devctl apex clear sbx1` writing `devctl: apex clear takes no arguments` as the first of the same three lines, each with nothing on stdout and exit 2.

- R-4D75-ZO9I: After its arguments are accepted, every `apex` subcommand MUST call `checkout.ReadRootFile(ctx, deps)` and return its error unchanged; `set` MUST then obtain the app and the space from its operand by `spaceref.ParseApp` as D04 R-QW0J-KUFF requires, returning its error unchanged; every subcommand MUST then call `cloud.Connect(ctx, deps.Cloud, root.Domain, root.Region)` with the `Domain` and `Region` of the `RootFile` it read, as D02 R-OO0I-HZUA binds, and return its error unchanged, making no later call when any of these fails; in the requirements below, `root` is that `Domain`, `app` is the `spaceref.App` `ParseApp` returned, `target` is the `cloud.Space` `cloud.LookupSpace` returned for `app.Space.Domain`, `clients` is the `Clients` of the `Session` `Connect` returned, `zone` is what `clients.Route53.Zone(ctx, root)` returned, `apex` is the `space.ApexRecord` that `space.FindApex(ctx, clients.Route53, clients.EC2, zone.ID, root)` returned, and `holder` is the `cloud.Space` `cloud.LookupSpace` returned for `apex.Holder`; verified at least through `cli.Run` by `devctl apex set crm.sbx1.example.com` in a temporary checkout whose root file names `ikigenba.dev` writing the single stderr line `devctl: 'crm.sbx1.example.com' is not an app on a space: <app>.<space>` with empty stdout, exit 2, and a recording fake `Deps.Cloud` left with no call, and by `devctl apex show` in one whose root file holds `{"domain": "example.test", "region": "eu-west-1"}` leaving that fake with exactly one call whose profile is `example.test` and whose region is `eu-west-1`.

- R-4FMY-R7QW: `apex set` MUST, after `Connect`, call `cloud.LookupSpace(ctx, clients.EC2, root, app.Space.Domain)` and return its error unchanged, so that a `*cloud.NoSpaceError` reaches `cli.Run` as D03 R-R10I-DEAA describes; MUST then return a `*space.NotRunningError` whose `Domain` is `target.Domain` and whose `State` is `target.State` when that state is not `cloud.StateRunning`; MUST then call `clients.Route53.Zone(ctx, root)` and then `space.FindApex(ctx, clients.Route53, clients.EC2, zone.ID, root)`, returning each error unchanged; MUST then return a `*NotSpaceAddressError` whose `Root` is `root` and whose `Values` are `apex.Record.Values` when `apex.Found` is true and `apex.Holder` is empty; and, when `apex.Holder` is neither empty nor equal to `target.Domain`, MUST then obtain the holder's `spaceref.Space` by `spaceref.Parse(apex.Holder, root)`, call `cloud.LookupSpace(ctx, clients.EC2, root, apex.Holder)`, return each call's error unchanged, and return a `*HolderStoppedError` whose `Domain` is `holder.Domain`, whose `Label` is that `spaceref.Space`'s `Label`, and whose `State` is `holder.State` when that state is not `cloud.StateRunning`; every one of these MUST complete before any byte is written to `stdout`, before any `seam.Cmd` other than the one `checkout.Open` passes reaches `deps.Exec` or `deps.Stream`, and before any call to a method of `clients` that creates, changes, or deletes anything; verified at least through `cli.Run`, in a temporary checkout whose root file holds `{"domain": "ikigenba.dev", "region": "us-east-2"}`, by `devctl apex set crm.gone` writing the single stderr line `devctl: no space at 'gone.ikigenba.dev'` with a fake `EC2` listing no instance tagged `Space=gone.ikigenba.dev`, by `devctl apex set crm.sbx2` writing `devctl: 'sbx2.ikigenba.dev' is stopped` with a fake `EC2` whose `sbx2.ikigenba.dev` instance is `stopped`, by `devctl apex set crm.sbx1`, with `sbx1.ikigenba.dev` running and the zone's `A` record `ikigenba.dev` holding `203.0.113.9`, an `IP` no address carries, writing `devctl: ikigenba.dev points at 203.0.113.9, which is not a space's address`, and by `devctl apex set crm.sbx1`, with that record holding the `IP` of the address whose `Space` is `sbx2.ikigenba.dev` and that instance `stopped`, writing exactly the three lines `devctl: 'sbx2.ikigenba.dev' holds the apex and is stopped`, an empty line, and `run 'devctl space start sbx2' first`, each with empty stdout, exit 1, no `ssh` `seam.Cmd` passed to `deps.Exec`, and `PutRolePolicy` and `ChangeRecords` not called.

- R-4GUV-4ZHL: `apex set` MUST write, as its only stdout, the step lines `space`, `role`, `host`, `record`, and `previous`, in that order, each written with `space.Step` when its step has completed, followed by the line `root`, one space, and `app.Hostname`, and MUST exit 0 only after every step has completed; it MUST perform every step on every run, whether or not `target` already holds the apex; and it MUST stop at the first failure, returning that step's error, without writing a line for the failed step, without undoing a completed step, and without writing the final line; verified at least by reproducing, in order, `space: ok (sbx1.ikigenba.dev running, 18.118.7.42)`, `role: ok (sbx1.ikigenba.dev may prove ikigenba.dev)`, `host: ok (host.apex=crm, certificate obtained, nginx applied)`, `record: ok (ikigenba.dev -> 18.118.7.42, INSYNC)`, `previous: ok (sbx2.ikigenba.dev cleared)`, and `ikigenba.dev crm.sbx1.ikigenba.dev` with empty stderr and exit 0 for both `devctl apex set crm.sbx1` and `devctl apex set crm.sbx1.ikigenba.dev` when the record held the `IP` of the running `sbx2.ikigenba.dev`, and by reproducing the same six lines with `previous: ok (none)` as the fifth both when the zone held no `A` record `ikigenba.dev` and when it held `target.Address`.

- R-4I2R-IR8A: The `space` step of `apex set` MUST be written first, after the checks R-4FMY-R7QW states and before any other call, with the detail `target.Domain`, ` running, `, and `target.Address` joined.

- R-VX21-6EX8: The `role` step of `apex set` MUST call `clients.IAM.PutRolePolicy(ctx, space.RoleName(app.Space.Domain), space.PolicyName, space.PolicyDocument(root, zone.ID, app.Space, true))` exactly once and, when it returns a nil error, write the step `role` with the detail `target.Domain`, ` may prove `, and `root` joined; a non-nil error MUST be returned unchanged, so that a `*cloud.Error` reaches `cli.Run` as D03 R-QZSL-ZMJL describes; verified at least by a recording fake `IAM` left with exactly one `PutRolePolicy` call whose role is `sbx1.ikigenba.dev`, whose policy is `space`, and whose document equals `space.PolicyDocument("ikigenba.dev", zone.ID, app.Space, true)` for that zone and space, and through `cli.Run` by a `PutRolePolicy` that fails with a `*cloud.Error` for `iam PutRolePolicy` with `Code` `Throttling` leaving exactly `space: ok (sbx1.ikigenba.dev running, 18.118.7.42)` on stdout, the single stderr line `devctl: iam PutRolePolicy: Throttling`, exit 1, and no `ssh` `seam.Cmd` passed to `deps.Exec`.

- R-CFMJ-PD79: The `host` step of `apex set` MUST run on a `host.Host` whose `Address` is `target.Address` and whose `Deps` is `deps`, and MUST consist of, in order, exactly one call to `(host.Host).Wait`, then `hostsetup.SetKey(ctx, h, "host", hostsetup.KeyHostApex, app.Name)`, then one call to `(host.Host).Sudo` with the step `host` and exactly the arguments `opsctl`, `cert`, and `obtain`, then one call to `(host.Host).Sudo` with the step `host` and exactly the arguments `opsctl`, `nginx`, and `apply`, issuing no later one once one has failed; a non-nil error from `Wait` MUST be returned as a `*StepError` whose `Step` is `host` and whose `Err` is that error, and a non-nil error from any of the three commands MUST be returned unchanged, so that a `*host.CommandError` reaches `cli.Run` as D06 R-D9BO-1R6T and D05 R-D4G2-IO81 describe; on success the step's detail MUST be `host.apex=`, `app.Name`, and `, certificate obtained, nginx applied` joined; verified at least through `cli.Run` by `devctl apex set crm.sbx1` with a fake `Deps.Exec` whose `sudo opsctl cert obtain` process exits 1 writing nothing to its standard output and to its standard error exactly the four lines `opsctl: certbot certonly: exit status 1`, an empty line, `> Some challenges have failed.`, and `> Detail: DNS problem: NXDOMAIN looking up TXT for _acme-challenge.ikigenba.dev`, each followed by a newline, leaving exactly `space: ok (sbx1.ikigenba.dev running, 18.118.7.42)` and `role: ok (sbx1.ikigenba.dev may prove ikigenba.dev)` on stdout and, on stderr, exactly the six lines `devctl: host: ssh ec2-user@18.118.7.42 sudo opsctl cert obtain: exit status 1`, an empty line, `> opsctl: certbot certonly: exit status 1`, `> `, `> > Some challenges have failed.`, and `> > Detail: DNS problem: NXDOMAIN looking up TXT for _acme-challenge.ikigenba.dev`, with exit 1, `ChangeRecords` not called, no `ssh` `seam.Cmd` to any address other than `18.118.7.42`, and the recorded remote argument vectors of that address being exactly, in order, `true`; `sudo`, `opsctl`, `config`, `set`, `host.apex=crm`; and `sudo`, `opsctl`, `cert`, `obtain`.

- R-4LQG-O2GD: The `record` step of `apex set` MUST call `clients.Route53.ChangeRecords` for `zone.ID` exactly once, with exactly one `cloud.RecordChange` whose `Action` is `cloud.ChangeUpsert` and whose `Record` has `root` as `Name`, `A` as `Type`, `space.RecordTTL` as `TTL`, and exactly `target.Address` as `Values`; MUST then call `space.WaitInsync(ctx, deps, clients.Route53, changeID)` with the change id that call returned; MUST write the step `record` with the detail `root`, ` -> `, `target.Address`, and `, INSYNC` joined only after `WaitInsync` returned a nil error; MUST return a non-nil error from either unchanged; and MUST NOT change any record other than the `A` record at `root`, in particular never one named `*.` followed by `root`; verified at least by reproducing `record: ok (ikigenba.dev -> 18.118.7.42, INSYNC)` with a recording fake `Route53` left with exactly one `ChangeRecords` call carrying that one change, and through `cli.Run` by a `ChangeRecords` that fails with a `*cloud.Error` for `route53 ChangeResourceRecordSets` with `Code` `Throttling` leaving the `space`, `role`, and `host` lines on stdout, the single stderr line `devctl: route53 ChangeResourceRecordSets: Throttling`, and exit 1.

- R-VY9X-K6NX: The `previous` step of `apex set` MUST, when `apex.Holder` is neither empty nor equal to `target.Domain`, call `clients.IAM.PutRolePolicy(ctx, space.RoleName(apex.Holder), space.PolicyName, space.PolicyDocument(root, zone.ID, prev, false))` exactly once, where `prev` is the holder's `spaceref.Space` R-4FMY-R7QW obtained, and then, on a `host.Host` whose `Address` is `holder.Address` and whose `Deps` is `deps`, make exactly one call to `(host.Host).Wait`, then `hostsetup.DelKey(ctx, h, "previous", hostsetup.KeyHostApex)`, then one call to `(host.Host).Sudo` with the step `previous` and exactly the arguments `opsctl`, `cert`, and `obtain`, then one call to `(host.Host).Sudo` with the step `previous` and exactly the arguments `opsctl`, `nginx`, and `apply`, issuing no later call once one has failed, returning a `Wait` failure as a `*StepError` whose `Step` is `previous` and whose `Err` is that error and every other failure unchanged, and on success write the step `previous` with the detail `apex.Holder` followed by ` cleared`; otherwise it MUST write the step `previous` with the detail `none`, call `PutRolePolicy` for no role other than `space.RoleName(app.Space.Domain)`, and pass no `ssh` `seam.Cmd` to any address other than `target.Address`; verified at least through `cli.Run` by `devctl apex set crm.sbx1`, when the record held the `IP` `18.220.10.5` of the running `sbx2.ikigenba.dev`, with a fake `Deps.Exec` whose every `ssh` process to `ec2-user@18.220.10.5` exits 255 and a fake `Deps.After` that fires immediately, leaving the `space`, `role`, `host`, and `record` lines on stdout, the single stderr line `devctl: previous: ssh ec2-user@18.220.10.5: connection timed out`, and exit 1, with `PutRolePolicy` called for `sbx2.ikigenba.dev` with the document `space.PolicyDocument("ikigenba.dev", zone.ID, prev, false)` and no remote argument vector beginning `sudo` recorded for that address.

- R-4O69-FLXR: `apex set` MUST call no method of `clients.SSM` or `clients.S3`, no method of `clients.EC2` other than those `cloud.LookupSpace` and `space.FindApex` make, no method of `clients.Route53` other than `Zone`, those `space.FindApex` makes, the `record` step's `ChangeRecords`, and the `ChangeStatus` calls `space.WaitInsync` makes, and no method of `clients.IAM` other than the `role` and `previous` steps' `PutRolePolicy`; MUST NOT call `(*Checkout).Apps` or `(*Checkout).App`, read any file under the checkout root other than the root file, or require `app.Name` to name a directory or manifest in the checkout; and MUST pass to `deps.Exec` no `seam.Cmd` beyond the one `checkout.ReadRootFile` passes and the `ssh` commands the `host` and `previous` steps state, and none to `deps.Stream`, so that `set` neither deploys, restarts, nor stops anything; verified at least by a successful `devctl apex set crm.sbx1` in a temporary checkout that holds a root file and no `crm` directory, with recording fake clients and a recording fake `Deps.Exec` left with no other call.

- R-4PE5-TDOG: `apex show` MUST, after `Connect`, call `clients.Route53.Zone(ctx, root)` and then `space.FindApex(ctx, clients.Route53, clients.EC2, zone.ID, root)`, returning each error unchanged; when `apex.Found` is false it MUST write nothing to stdout, return a nil error, call `cloud.LookupSpace` not at all, and pass no `seam.Cmd` to `deps.Exec` other than `checkout.Open`'s; when `apex.Found` is true and `apex.Holder` is empty it MUST return a `*NotSpaceAddressError` whose `Root` is `root` and whose `Values` are `apex.Record.Values`; otherwise it MUST call `cloud.LookupSpace(ctx, clients.EC2, root, apex.Holder)`, return its error unchanged, and return a `*space.NotRunningError` whose `Domain` is `holder.Domain` and whose `State` is `holder.State` when that state is not `cloud.StateRunning`; each of these before any `ssh` `seam.Cmd` reaches `deps.Exec`; verified at least through `cli.Run`, in a temporary checkout whose root file names `ikigenba.dev`, by `devctl apex show` writing nothing to either stream with exit 0 when the zone holds no `A` record `ikigenba.dev`, writing the single stderr line `devctl: ikigenba.dev points at 203.0.113.9, which is not a space's address` with empty stdout and exit 1 when that record holds `203.0.113.9`, an `IP` no address carries, and writing the single stderr line `devctl: 'sbx1.ikigenba.dev' is stopped` with empty stdout and exit 1 when it holds the `IP` of the address whose `Space` is `sbx1.ikigenba.dev` and that instance is `stopped`, none of the three passing an `ssh` `seam.Cmd` to `deps.Exec`.

- R-4QM2-75F5: When `holder.State` is `cloud.StateRunning`, `apex show` MUST obtain the app by exactly one call to `hostsetup.GetKey(ctx, h, "show", hostsetup.KeyHostApex)` on a `host.Host` whose `Address` is `holder.Address` and whose `Deps` is `deps`, which MUST be the only `seam.Cmd` beyond `checkout.Open`'s that it passes to `deps.Exec`; when `GetKey` reports the key set it MUST write to stdout exactly one line consisting of the value, `.`, `holder.Domain`, one space, and `holder.Address`, and return a nil error; when `GetKey` reports the key unset it MUST return a `*NoApexAppError` whose `Root` is `root` and whose `Domain` is `holder.Domain`; and a non-nil error from `GetKey` MUST be returned unchanged; verified at least through `cli.Run` by reproducing the single stdout line `crm.sbx1.ikigenba.dev 18.118.7.42` with empty stderr and exit 0 for a space at `18.118.7.42` and a fake `ssh` process that exits 0 with standard output `crm` followed by a newline and a recorded remote argument vector of exactly `sudo`, `opsctl`, `config`, `get`, and `host.apex`, and by reproducing the single stderr line `devctl: ikigenba.dev points at sbx1.ikigenba.dev, whose host.apex is not set` with empty stdout and exit 1 for one that exits 1.

- R-4RTY-KX5U: `apex show` MUST call no method of `clients.SSM`, `clients.S3`, or `clients.IAM`, no method of `clients.EC2` other than those `space.FindApex` and `cloud.LookupSpace` make, no method of `clients.Route53` other than `Zone` and those `space.FindApex` makes, and no method of any client that creates, changes, or deletes anything, verified with recording fake clients that fail on mutation, for a record that is absent and for one that names a running holder.

- R-4T1U-YOWJ: `apex clear` MUST, after `Connect`, call `clients.Route53.Zone(ctx, root)` and then `space.FindApex(ctx, clients.Route53, clients.EC2, zone.ID, root)`, returning each error unchanged; when `apex.Found` is false it MUST write exactly the line `record: ok (already gone)` with `space.Step`, return a nil error, call `ChangeRecords` and `cloud.LookupSpace` not at all, and pass no `seam.Cmd` to `deps.Exec` other than `checkout.Open`'s; when `apex.Found` is true and `apex.Holder` is empty it MUST call `clients.Route53.ChangeRecords` for `zone.ID` exactly once with one `cloud.RecordChange` whose `Action` is `cloud.ChangeDelete` and whose `Record` is `apex.Record`, then write exactly the line `record: ok (<root> deleted)` with `space.Step`, return a nil error, and pass no such `seam.Cmd`; otherwise it MUST obtain the holder's `spaceref.Space` by `spaceref.Parse(apex.Holder, root)`, call `cloud.LookupSpace(ctx, clients.EC2, root, apex.Holder)`, return each call's error unchanged, and return a `*space.NotRunningError` whose `Domain` is `holder.Domain` and whose `State` is `holder.State` when that state is not `cloud.StateRunning`, before any byte is written to stdout, before `ChangeRecords` or `PutRolePolicy` is called, and before any `ssh` `seam.Cmd` reaches `deps.Exec`, so that the record is kept; verified at least through `cli.Run`, in a temporary checkout whose root file names `ikigenba.dev`, by reproducing `record: ok (already gone)` as the only output with exit 0 when the zone holds no `A` record `ikigenba.dev`, by reproducing `record: ok (ikigenba.dev deleted)` as the only output with exit 0 and exactly one `ChangeRecords` call when it holds `203.0.113.9`, an `IP` no address carries, and by the single stderr line `devctl: 'sbx1.ikigenba.dev' is stopped` with empty stdout, exit 1, and `ChangeRecords` not called when it holds the `IP` of the address whose `Space` is `sbx1.ikigenba.dev` and that instance is `stopped`, none of the three passing an `ssh` `seam.Cmd` to `deps.Exec`.

- R-4U9R-CGN8: When `holder.State` is `cloud.StateRunning`, `apex clear` MUST write, as its only stdout, the step lines `space`, `record`, `role`, and `host`, in that order, each written with `space.Step` when its step has completed, MUST write no further line, MUST exit 0 only after every step has completed, and MUST stop at the first failure, returning that step's error, without writing a line for the failed step and without undoing a completed step; the `space` step's detail MUST be `holder.Domain`, ` running, `, and `holder.Address` joined; verified at least by reproducing, in order, `space: ok (sbx1.ikigenba.dev running, 18.118.7.42)`, `record: ok (ikigenba.dev deleted)`, `role: ok (sbx1.ikigenba.dev may no longer prove ikigenba.dev)`, and `host: ok (host.apex removed, certificate obtained, nginx applied)` as the whole of stdout with empty stderr and exit 0.

- R-4VHN-Q8DX: The `record` step of `apex clear` MUST call `clients.Route53.ChangeRecords` for `zone.ID` exactly once with one `cloud.RecordChange` whose `Action` is `cloud.ChangeDelete` and whose `Record` is `apex.Record` unchanged, MUST NOT call `ChangeStatus` or `space.WaitInsync` for that change, MUST write the step `record` with the detail `root` followed by ` deleted` when the call returns a nil error, and MUST return its error unchanged otherwise; verified at least by a recording fake `Route53` left with exactly one `ChangeRecords` call whose one change carries the `cloud.Record` its `FindRecord` returned and no `ChangeStatus` call.

- R-VZHT-XYEM: The `role` step of `apex clear` MUST call `clients.IAM.PutRolePolicy(ctx, space.RoleName(apex.Holder), space.PolicyName, space.PolicyDocument(root, zone.ID, prev, false))` exactly once, where `prev` is the holder's `spaceref.Space` R-4T1U-YOWJ obtained, MUST write the step `role` with the detail `holder.Domain`, ` may no longer prove `, and `root` joined when it returns a nil error, and MUST return its error unchanged otherwise; verified at least by a recording fake `IAM` left with exactly one `PutRolePolicy` call whose role is `sbx1.ikigenba.dev`, whose policy is `space`, and whose document equals `space.PolicyDocument("ikigenba.dev", zone.ID, prev, false)` for that zone and space.

- R-JUK4-3U10: The `host` step of `apex clear` MUST run on a `host.Host` whose `Address` is `holder.Address` and whose `Deps` is `deps`, and MUST consist of, in order, exactly one call to `(host.Host).Wait`, then `hostsetup.DelKey(ctx, h, "host", hostsetup.KeyHostApex)`, then one call to `(host.Host).Sudo` with the step `host` and exactly the arguments `opsctl`, `cert`, and `obtain`, then one call to `(host.Host).Sudo` with the step `host` and exactly the arguments `opsctl`, `nginx`, and `apply`, issuing no later one once one has failed, returning a `Wait` failure as a `*StepError` whose `Step` is `host` and whose `Err` is that error and every other failure unchanged; on success its detail MUST be `host.apex removed, certificate obtained, nginx applied`; verified at least by a fake `Deps.Exec` left with the remote argument vectors, in order, `true`; `sudo`, `opsctl`, `config`, `del`, `host.apex`; `sudo`, `opsctl`, `cert`, `obtain`; and `sudo`, `opsctl`, `nginx`, `apply`, and through `cli.Run` by a `sudo opsctl nginx apply` process that exits 1 writing nothing to its standard output and to its standard error exactly the three lines `opsctl: nginx -t: exit status 1`, an empty line, and `> nginx: configuration file /etc/nginx/nginx.conf test failed`, each followed by a newline, leaving the `space`, `record`, and `role` lines on stdout and, on stderr, exactly the five lines `devctl: host: ssh ec2-user@18.118.7.42 sudo opsctl nginx apply: exit status 1`, an empty line, `> opsctl: nginx -t: exit status 1`, `> `, and `> > nginx: configuration file /etc/nginx/nginx.conf test failed`, with exit 1, for a holder at `18.118.7.42`.

- R-50D9-9BCP: `apex clear` MUST call no method of `clients.SSM` or `clients.S3`, no method of `clients.EC2` other than those `space.FindApex` and `cloud.LookupSpace` make, no method of `clients.Route53` other than `Zone`, those `space.FindApex` makes, and the `record` step's `ChangeRecords`, and no method of `clients.IAM` other than the `role` step's `PutRolePolicy`, and MUST pass to `deps.Exec` no `seam.Cmd` beyond `checkout.Open`'s and the `host` step's `ssh` commands, and none to `deps.Stream`, so that the holder's apps, its own two records, its secrets, and its backups are left as they were; verified with recording fake clients and a recording fake `Deps.Exec` left with no other call after a successful `devctl apex clear`.
