#!/bin/bash

# bin's toolchain commands (go build/test ./bin/bintest/...) are repo-root
# relative, since bintest has no module boundary of its own — it rides the
# root go.work. Run this wrapper from the repo root, not from bin/.
exec ralph project/loops/gather.md project/loops/build.md project/loops/verify.md
