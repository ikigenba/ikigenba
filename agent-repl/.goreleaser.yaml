version: 2

project_name: agent-repl

builds:
  - id: agent-repl
    main: ./cmd/agent-repl
    binary: agent-repl
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w

archives:
  - id: agent-repl
    formats: [tar.gz]
    name_template: "{{ .Binary }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

# The workflow publishes the release itself (gh release create --generate-notes)
# on the real monorepo tag; goreleaser only builds and archives.
changelog:
  disable: true
