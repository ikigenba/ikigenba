# dashboard — Plan Status (web pages restructure)

This is the **manifest**: one line per phase in build order, and the **only** place
a phase's status marker lives. Each phase line begins with the literal word `Phase`
and carries `✅` (done) or `⬜` (not started). The build loop finds its next unit of
work with `grep -nE '^Phase .* ⬜' project/plan/STATUS.md | head -1`, reads only
that phase's `project/plan/phase-NN.md`, builds it, and on completion flips that one
marker. This file deliberately carries **no bare status glyph** anywhere but on a
phase line, so the anchored grep matches only phase lines.

Phase 01 ✅ realizes D1, D2 — add the `/profile` route + page (session-gated, redirect-when-signed-out) and confirm the login/landing topology on `/`
Phase 02 ✅ realizes D3 — move personal-access-token management (form + list + revoke) onto the profile page; redirect PAT actions to `/profile`; remove PATs from the landing
Phase 03 ✅ realizes D4 — move OAuth grant management (live block + SSE + revoke) onto the profile page; redirect grant revoke to `/profile`; remove grants from the landing
Phase 04 ✅ realizes D5 — landing composition: link each service name to `/srv/<svc>/`, link the owner email to `/profile`, keep sign-out and install instructions on the landing
Phase 05 ✅ realizes D6 — purge the stale "single hybrid page / don't split" rule from `dashboard/AGENTS.md` and state the three-page truth
Phase 06 ✅ realizes D7 — login composition: keep the control-plane tagline + sign-in CTA verbatim and add a name-origin colophon below the CTA on the logged-out `/` that explains the ikigenba portmanteau as a name + its two subcomponents (ikigai, genba)
Phase 07 ✅ realizes D8 — eliminate the web-font FOUT: switch the four @font-face blocks in tokens.css to font-display: optional and preload the display + body fonts (crossorigin) in both head-bearing templates (index.html, profile.html); apex src already origin-correct; covers R-P97M-GIJ1, R-PAFI-UA9Q, R-PBNF-820F
Phase 08 ✅ realizes D7 — reword the logged-out login sub-line to "Sign in to access your services." (replacing the stale tokens/agents/MCP enumeration) in index.html and update the pinned assertion in index_test.go; covers R-DB18-KEEP
Phase 09 ✅ realizes D7 — tighten the name-origin colophon copy (drop the "livelihood" lede gloss, shorten both part glosses) AND add a pronunciation guide ("EE-kee-GEN-buh") at the foot of the colophon, in index.html + app.css; update the pinned assertions in index_test.go; covers R-DB17-ORIG, R-O7K1-XEN7
Phase 10 ✅ realizes D7, D9 — make "Sign in to access your services." the login `<h1>` and remove the "Your account's control plane" heading (index.html + index_test.go), AND remove the site-wide footer from every template (index login+landing, profile, pat_created) and delete its `.site-footer` CSS; covers R-DB18-KEEP, R-EFJZ-FRQ1
Phase 11 ✅ realizes D5 — replace the banner email text link with a monogram profile avatar (solid accent circle + uppercased first-letter initial) linking to `/profile`, furthest right after sign-out, full email carried as aria-label/title; add an `OwnerInitial` view field + `ownerInitial` helper in index.go, an `.avatar` CSS rule (remove dead `.identity .owner`), and update landing_composition_test.go; covers R-XO4W-LKAI
Phase 12 ✅ realizes D10 — shared banner chrome: make the signed-in wordmark a home link (`<a href="/" class="wordmark">`) on landing + profile (login page keeps `<p>`); rewrite the profile banner to sign-out + monogram avatar (drop the bordered Home button and `<span class="owner">` email text), add `OwnerInitial` to `profileData` via the `ownerInitial` helper; style the wordmark link (no underline, text color) and add `.identity .avatar:hover` darkening to `--accent-700`; update the profile/landing banner tests; covers R-VTIE-IUFA, R-VUQA-WM5Z, R-VVY7-ADWO
Phase 13 ✅ realizes D5 — service list adopts the shared `.list`/`.row` chrome instead of the one-off `<table>` (name link left, MCP URL + copy button right, no header row) and each row's raw MCP URL gets a reused `.copy-btn`; remove both `.section-intro` paragraphs (connect-agent + services) from the signed-in landing; delete the `.services-table` CSS, add the `.service-row` copy-button/URL treatment, generalize the app.js `.copy-btn` source lookup to `.snippet, .service-row`; update landing_composition_test.go + connect_section_test.go; covers R-OF1Q-VEDC, R-OG9N-9641, R-OHHJ-MXUQ
Phase 14 ✅ realizes D7 — refine the logged-out name-origin colophon copy: drop "Japanese" from the lede ("A portmanteau of two words:"), quote both `<dd>` glosses in curly quotes and swap each `;` for a spaced em-dash, and center the `.name-origin-say` pronunciation foot (`text-align: center`); update the pinned name-origin assertions in index_test.go + add a served-CSS centering assertion; refines R-DB17-ORIG, R-O7K1-XEN7 (no new ids)
Phase 15 ✅ realizes D11, D12 — new `internal/telemetry` package: the in-memory ring-buffer `Store` (1440-sample series, recorded capacities, isolated `Snapshot`) plus the metric readers (`/proc/meminfo` MemAvailable/MemTotal, `statfs` free/total of `/opt`, cgroup `memory.current` per `<svc>.service`, `du` of `/opt/<svc>`) and MCP service discovery via `inventory.Read` (dashboard excluded), each over injected roots with absent-path→0; covers R-EZVQ-IQOL, R-F13M-WIFA, R-F2BJ-AA5Z, R-F4RC-1TND, R-F5Z8-FLE2, R-F774-TD4R, R-F8F1-74VG, R-F9MX-KWM5, R-FAUT-YOCU, R-FC2Q-CG3J
Phase 16 ✅ realizes D13 — the collector: `Collect` (one sample sweep, unexpected-error→0+log) and `Run` (immediate first sample, then 60s ticker, clean nil return on ctx cancel), wired into `cmd/dashboard/main.go` as an `appkit.Spec.Workers` closure over a shared `Store` via the `rt`-capture idiom; covers R-FDAM-Q7U8, R-FEIJ-3ZKX, R-FFQF-HRBM, R-FGYB-VJ2B
Phase 17 ✅ realizes D15 — pure SVG chart builders in `internal/telemetry`: two hero line charts (y-axis 0→total capacity, current-value label, native `<title>` hover), two accumulated stacked-area charts (top-7 services + "Other", light fills, legend, committed CVD-validated `bandPalette` distinct per band), and the `humanBytes` binary formatter; covers R-FO9Q-65IH, R-FPHM-JX96, R-FQPI-XOZV, R-FRXF-BGQK, R-FT5B-P8H9, R-FUD8-307Y, R-FVL4-GRYN
Phase 18 ✅ realizes D14 — the HTTP surface: `server.Options.Telemetry` + `*app` field, `GET /telemetry` (session-gated, 303→/ when signed out) and `GET /telemetry/fragment` (401 when signed out) rendering `telemetry.html`/`telemetry_charts.tmpl` from a `Snapshot`, the store threaded from main into `server.Register`, and the 60s `setInterval` telemetry poll in `app.js`; covers R-FI68-9AT0, R-FJE4-N2JP, R-FKM1-0UAE, R-FLTX-EM13, R-FN1T-SDRS
Phase 19 ✅ realizes D16 — a landing tile linking to `/telemetry` in the signed-in `{{if .Owner}}` branch of `index.html` (reusing existing tile/list chrome), absent from the logged-out login page; covers R-FWT0-UJPC, R-FY0X-8BG1
Phase 20 ✅ realizes D17 — new `internal/identity` package + `identities` table migration: the `Store` (`ResolveOrCreate` upsert on `UNIQUE (iss, sub)`, `Lookup`) keyed by `(iss, sub)` behind an opaque `ids.New()` handle, with email/name/picture as attributes; covers R-VJMO-6CN9, R-VKUK-K4DY, R-VM2G-XW4N, R-VNAD-BNVC, R-VOI9-PFM1
Phase 21 ✅ realizes D18 (decode slice) — `internal/googleidp` decodes `iss`/`name`/`picture` from the verified id_token onto `Identity` (absent name/picture → empty, still valid); covers R-VPQ6-37CQ
Phase 22 ✅ realizes D18 (stamp slice) — `owner_id TEXT` migration on `web_sessions`/`oauth_authcodes`/`oauth_chains`/`personal_tokens`; resolve the identity at callback and stamp the handle through session, authcode→chain, and PAT creation (owner_email unchanged); covers R-VQY2-GZ3F, R-VS5Y-UQU4, R-VTDV-8IKT
Phase 23 ✅ realizes D19 — emit `X-Owner-Id` + percent-encoded `X-Owner-Name`/`X-Owner-Picture` on the allow path of `handleAuthn`/`handleAuthnPAT`/`handleSessionAuthn`, existing headers unchanged, injection-safe, absence-tolerant (emission only; nginx/services out of scope); covers R-VULR-MABI, R-VX1K-DTSW, R-VY9G-RLJL, R-VZHD-5DAA, R-W0P9-J50Z
Phase 24 ⬜ realizes D20 — add the dashboard-owned apex `location @login_bounce` to `dashboard/etc/nginx.conf` (return-only `if`-form: navigate/absent `Sec-Fetch-Mode` → `302 /login?return_to=$request_uri`, `cors`/`same-origin`/`no-cors` → `401`), proven by a `cmd/dashboard` content-assertion test reading `etc/nginx.conf`; no service fragment or dev-mirror touched; covers R-XJBT-7YIF, R-XKJP-LQ94
Phase 25 ⬜ realizes D21 — add the nullable `return_to` `oauth_state` migration, `Handshake.ReturnTo` + `CreateWeb` + `Consume` readback in `internal/oauthstate`, and `handleLogin` capture through a `safeReturnTo` same-site validator (hostile values → empty); real-temp-DB round-trip + HTTP-level login tests; covers R-XLRL-ZHZT, R-XO7E-R1H7, R-XPFB-4T7W
Phase 26 ⬜ realizes D22 — `callbackWeb` redirects to the consumed handshake's `return_to` when non-empty else `/`, no re-validation, MCP-origin branch untouched; HTTP-level `Location`-header tests; covers R-XQN7-IKYL, R-XRV3-WCPA
