# Backup source inventory

Non-normative source block locators; acceptance details within each block are checked by scoped verifiers. Source blocks include introduction, commands, output, exit/stream rules, preconditions and postconditions.

| Locator | Source lines | Section | Source block | Verification |
|---|---|---|---|---|
| BACKUP-001 | specs/stories/backup.md:1–1 | Introduction | # Stories — backup | Awaiting scope verifier |
| BACKUP-002 | specs/stories/backup.md:3–6 | Introduction | A host is disposable; what it holds is not. Everything worth keeping goes to one S3 prefix that belongs to this host and no other, and comes back from there. Nothing else on the host reads or writes that prefix, and the host's own role can reach no other space's. | Awaiting scope verifier |
| BACKUP-003 | specs/stories/backup.md:8–11 | Introduction | Two different things are kept, so there are two pairs of commands. A **service** is what lives under `/opt/<name>/`; the **host** is the machine's own configuration and its certificate. A service backup never touches `/etc/`, and a host restore never touches `/opt/`. | Awaiting scope verifier |
| BACKUP-004 | specs/stories/backup.md:13–16 | Introduction | A host about to be discarded has one more thing to do: `opsctl retire` stops everything, lets litestream ship what it holds, and takes the service and host backups one last time, so a destroy loses nothing the timers had not yet copied. | Awaiting scope verifier |
| BACKUP-005 | specs/stories/backup.md:18–18 | Introduction | The top-level usage gains four lines under `Commands:`: | Awaiting scope verifier |
| BACKUP-006 | specs/stories/backup.md:20–25 | Introduction | ``` backup    back up a service's files to S3 host      back up and restore the host's own configuration restore   restore a service from its backups retire    stop every service and take the host's final backup ``` | Awaiting scope verifier |
| BACKUP-007 | specs/stories/backup.md:27–27 | Introduction | Configuration keys: | Awaiting scope verifier |
| BACKUP-008 | specs/stories/backup.md:29–36 | Introduction | \| key \| value \| \|---\|---\| \| `aws.region` \| the region the backup bucket lives in, e.g. `us-east-2` \| \| `backup.s3_uri` \| the prefix this host backs up to, e.g. `s3://sbx-ikigenba-dev-602773793009/foo.sbx.ikigenba.dev/` \| \| `backup.host_files_seconds` \| how often the host's own configuration is copied; `0` or unset means never \| \| `backup.service_files_seconds` \| how often every service's files are copied; `0` or unset means never \| \| `backup.service_db_seconds` \| how often a declared database is snapshotted whole \| \| `backup.service_wal_seconds` \| how often a declared database's committed changes are shipped \| | Awaiting scope verifier |
| BACKUP-009 | specs/stories/backup.md:40–42 | Two mechanisms, and where the line between them falls | A service's files are copied on a timer: `opsctl backup` tars `etc/` and `state/` and writes one object. That is a walk of the filesystem, and it is honest about being one — it holds whatever was on disk while it ran. | Awaiting scope verifier |
| BACKUP-010 | specs/stories/backup.md:44–49 | Two mechanisms, and where the line between them falls | A SQLite database cannot be backed up that way. Copying the file while a writer is mid-transaction yields a torn file, and its `-wal` companion is a separate moving target that is meaningless without the exact database it belongs to. Worse, the application's own connection checkpoints and resets the WAL on its own schedule, so anything that only runs on a timer finds the history it needed already folded away. | Awaiting scope verifier |
| BACKUP-011 | specs/stories/backup.md:51–56 | Two mechanisms, and where the line between them falls | So a declared database is replicated by `litestream`, which runs continuously as `litestream.service`. Being resident is the whole point: it holds a read transaction open so the WAL cannot be reset past what it has not yet shipped, and it takes `wal_autocheckpoint` away from SQLite so it decides when frames are folded back. A one-shot run has no mark to hold and would lose transactions between invocations. | Awaiting scope verifier |
| BACKUP-012 | specs/stories/backup.md:58–60 | Two mechanisms, and where the line between them falls | `litestream` is on the host the way `nginx` and `certbot` are: it comes with the account's launch template, and `init` checks for it on PATH rather than installing it. opsctl drives it and never fetches it. | Awaiting scope verifier |
| BACKUP-013 | specs/stories/backup.md:62–65 | Two mechanisms, and where the line between them falls | The two mechanisms therefore do not overlap. For a service that declares a database, `opsctl backup` **excludes** the database file, its `-wal` and `-shm`, and litestream's own `.<name>-litestream` metadata directory. Those objects are litestream's and reach S3 by its path, not by the tarball's. | Awaiting scope verifier |
| BACKUP-014 | specs/stories/backup.md:67–72 | Two mechanisms, and where the line between them falls | A restore crosses that line, which is why `opsctl restore` stops units and `opsctl backup` never does. The files step replaces the whole of `state/`, and the database, its `-wal` and `-shm`, and the metadata directory live there and are not in the tarball — so that step deletes exactly what litestream has open, while the service's own process is holding the same file. Both have to be down before the files land, and the database is rebuilt before either comes back. | Awaiting scope verifier |
| BACKUP-015 | specs/stories/backup.md:74–76 | Two mechanisms, and where the line between them falls | `opsctl install` already restarts a service to upgrade it, so a restore doing the same is not a new kind of interruption. A restore is a brief outage, deliberately. | Awaiting scope verifier |
| BACKUP-016 | specs/stories/backup.md:78–84 | Two mechanisms, and where the line between them falls | One litestream covers the whole host, so taking it down for one service's restore stops replication for every database on it. **That window is accepted, and a litestream unit per database is deliberately not the answer.** Nothing is damaged by it: a checkpoint folds WAL frames into the database file rather than discarding them, so every committed transaction stays on disk, and no other service's files are read or written by a restore. What the window costs is that those transactions are not yet in S3. | Awaiting scope verifier |
| BACKUP-017 | specs/stories/backup.md:86–95 | Two mechanisms, and where the line between them falls | litestream closes the window itself when it comes back. If the WAL was not reset while it was gone it resumes from its last offset and the gap never existed. If an application did reset the WAL, litestream sees the salt or the offset disagree with its last LTX file and takes a fresh snapshot instead of failing — and a database restored under it, which is behind a replica holding a higher TXID, is a case it detects by that same route. Either way the database is fully replicated again shortly after the restore, and the only thing the gap costs is the ability to restore to an instant inside it. `--at` can name such an instant, and litestream answers with the closest point it holds at or before it, so a restore aimed into a gap lands earlier than it was aimed. | Awaiting scope verifier |
| BACKUP-018 | specs/stories/backup.md:97–98 | Two mechanisms, and where the line between them falls | What is left is losing the host *during* a restore an operator is running by hand. That is the accepted risk. | Awaiting scope verifier |
| BACKUP-019 | specs/stories/backup.md:100–106 | Two mechanisms, and where the line between them falls | **The two clocks are not synchronised, and are not meant to be.** A restore takes the newest files at or before the moment asked for and, separately, the database at that moment itself — so the database is typically newer than the files around it. The database is what matters; a missing file is a missing file. An app that needs an artifact to be transactionally consistent with its rows must keep that artifact **in** the database. | Awaiting scope verifier |
| BACKUP-020 | specs/stories/backup.md:108–111 | Two mechanisms, and where the line between them falls | There is no incremental. SQLite has no incremental backup primitive, and litestream's compaction merges files it has already shipped rather than creating a recovery point that did not exist. What can be restored from is a snapshot and the committed changes after it, and nothing else. | Awaiting scope verifier |
| BACKUP-021 | specs/stories/backup.md:115–123 | What is backed up, and what is not | - `/etc/ikigenba/` and `/etc/letsencrypt/` — the host's own configuration and its certificate — by `opsctl host backup`, under `host/`. - Every service's `etc/` and `state/`, under the service's own name, by `opsctl backup`. - Every declared database, under the service's own name, by `litestream`. - Never `cache/`: it is, by the name, reconstructible. - Never `/etc/nginx/` and never a unit file: both are generated from the configuration store and what is under `/opt`, so a restored host writes them again rather than carrying stale copies forward. | Awaiting scope verifier |
| BACKUP-022 | specs/stories/backup.md:125–126 | What is backed up, and what is not | A service is discovered, never registered: any `/opt/<name>/` holding an `etc/` or a `state/` directory. | Awaiting scope verifier |
| BACKUP-023 | specs/stories/backup.md:128–130 | What is backed up, and what is not | A service **declares a database** when its `etc/manifest.toml` holds a `[database]` table naming the `engine` and the `path`, relative to the service's own directory, of the database that engine owns: | Awaiting scope verifier |
| BACKUP-024 | specs/stories/backup.md:132–136 | What is backed up, and what is not | ```toml [database] engine = "sqlite" path = "state/crm.db" ``` | Awaiting scope verifier |
| BACKUP-025 | specs/stories/backup.md:138–143 | What is backed up, and what is not | This mirrors the split `nginx.md` already makes: a service is *discovered* by what is on disk, and gets more than the baseline only if its manifest asks for it. There, a manifest naming a `port` is what earns a server block. Here, a `[database]` table is what earns replication. A service with no manifest, or a manifest that declares no database, has its whole `state/` in the tarball and nothing else. | Awaiting scope verifier |
| BACKUP-026 | specs/stories/backup.md:145–151 | What is backed up, and what is not | `init`'s sequence gains two steps after `nginx.conf`: `litestream`, which writes `/etc/litestream.yml` from the declared databases and the two database periods and enables `litestream.service`, and `timers`, which writes the two service and timer pairs that run the two file backups at their periods. The same step writes the certificate renewal pair `certificates.md` describes; it is a timer on the host, so it is written where the timers are, but it has no period key and is always enabled. | Awaiting scope verifier |
| BACKUP-027 | specs/stories/backup.md:153–159 | What is backed up, and what is not | `init` is not the only writer of `/etc/litestream.yml`. The file is a pure function of the manifests under `/opt` and the two periods, so whatever changes a manifest regenerates it: `opsctl install` does, in its own `litestream` step (see `apps.md`), and `opsctl restore` does before it starts litestream again. Both leave `litestream.service` alone when the regenerated file is byte for byte the old one. `init` alone enables the unit; the others assume it is enabled, because `init` ran before any app reached the host. | Awaiting scope verifier |
| BACKUP-028 | specs/stories/backup.md:161–170 | What is backed up, and what is not | The generated file turns litestream's own retention off (`retention: enabled: false`). The host's role can write to the bucket but never delete from it, and litestream's retention deletes: left on, every retention pass would be refused and logged for the life of the host. Off, litestream never attempts a delete, and the bucket's expiry is the only retention there is. The consequence is that every snapshot and every change file stays until it expires, so `restore --at` can name any instant inside the account's expiry window, not only the last day. Verified against litestream 0.5.17, the version first boot installs, where the key guards every path that deletes from a replica. | Awaiting scope verifier |
| BACKUP-029 | specs/stories/backup.md:174–174 | An operator asks what `backup` can do | Command: | Awaiting scope verifier |
| BACKUP-030 | specs/stories/backup.md:176–178 | An operator asks what `backup` can do | ``` $ opsctl backup --help ``` | Awaiting scope verifier |
| BACKUP-031 | specs/stories/backup.md:180–182 | An operator asks what `backup` can do | ``` $ opsctl backup -h ``` | Awaiting scope verifier |
| BACKUP-032 | specs/stories/backup.md:184–184 | An operator asks what `backup` can do | Output: | Awaiting scope verifier |
| BACKUP-033 | specs/stories/backup.md:186–204 | An operator asks what `backup` can do | ``` Usage: opsctl backup [SERVICE]  Copy every service's etc/ and state/ to the prefix in backup.s3_uri, under the service's own name, or just SERVICE when one is named.  Never copied: cache/, anything opsctl generates, and -- for a service that declares a [database] -- the database file, its -wal and -shm, and its litestream metadata directory. Those are replicated continuously by litestream.service. The host's own /etc/ is 'opsctl host backup'.  A service declares its database with a [database] table in etc/manifest.toml naming its engine and its path. 'opsctl init' writes the timer that runs this at backup.service_files_seconds.  Configuration keys: aws.region      the region the backup bucket lives in backup.s3_uri   the prefix this host backs up to ``` | Awaiting scope verifier |
| BACKUP-034 | specs/stories/backup.md:206–206 | An operator asks what `backup` can do | Exits 0. The text is on stdout; stderr is empty. It prints for any user. | Awaiting scope verifier |
| BACKUP-035 | specs/stories/backup.md:208–208 | An operator asks what `backup` can do | Preconditions: | Awaiting scope verifier |
| BACKUP-036 | specs/stories/backup.md:210–210 | An operator asks what `backup` can do | - `opsctl` is installed on the host. | Awaiting scope verifier |
| BACKUP-037 | specs/stories/backup.md:212–212 | An operator asks what `backup` can do | Postconditions: | Awaiting scope verifier |
| BACKUP-038 | specs/stories/backup.md:214–214 | An operator asks what `backup` can do | - Nothing has changed. | Awaiting scope verifier |
| BACKUP-039 | specs/stories/backup.md:218–221 | The host backs up its services | `ikigenba-backup-services.timer` runs this every `backup.service_files_seconds`. One line per service, in name order, so the timer's mail — or an operator running it by hand — says exactly what was written. | Awaiting scope verifier |
| BACKUP-040 | specs/stories/backup.md:223–223 | The host backs up its services | Command: | Awaiting scope verifier |
| BACKUP-041 | specs/stories/backup.md:225–227 | The host backs up its services | ``` $ sudo opsctl backup ``` | Awaiting scope verifier |
| BACKUP-042 | specs/stories/backup.md:229–229 | The host backs up its services | Output: | Awaiting scope verifier |
| BACKUP-043 | specs/stories/backup.md:231–234 | The host backs up its services | ``` crm: ok (2026-09-12T03:00:04Z.tar.zst, 1.2 MiB) dashboard: ok (2026-09-12T03:00:04Z.tar.zst, 1.1 MiB) ``` | Awaiting scope verifier |
| BACKUP-044 | specs/stories/backup.md:236–236 | The host backs up its services | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-045 | specs/stories/backup.md:238–238 | The host backs up its services | Preconditions: | Awaiting scope verifier |
| BACKUP-046 | specs/stories/backup.md:240–243 | The host backs up its services | - `aws.region` and `backup.s3_uri` are set, and the host's role can write under that prefix. - Two services are installed. `/opt/crm/etc/manifest.toml` declares a `[database]` at `state/crm.db`; `dashboard`'s declares none. | Awaiting scope verifier |
| BACKUP-047 | specs/stories/backup.md:245–245 | The host backs up its services | Postconditions: | Awaiting scope verifier |
| BACKUP-048 | specs/stories/backup.md:247–256 | The host backs up its services | - `<backup.s3_uri><service>/2026-09-12T03:00:04Z.tar.zst` holds that service's `etc/` and `state/`, for each service. - `crm`'s object holds no `state/crm.db`, no `state/crm.db-wal`, no `state/crm.db-shm`, and nothing under `state/.crm.db-litestream/`. `dashboard`'s object holds all of its `state/`. - Every object of one run carries the same timestamp, so a run is one set. - Nothing on the host has changed, and no earlier backup was deleted or overwritten. What is kept for how long is the bucket's lifecycle policy, which infra owns and opsctl never touches. - Running it again writes a new set under a new timestamp. | Awaiting scope verifier |
| BACKUP-049 | specs/stories/backup.md:260–262 | An operator backs up one service | An operator about to do something risky to one service wants that service's files in the bucket now, and has no reason to wait for the timer or to touch the others. | Awaiting scope verifier |
| BACKUP-050 | specs/stories/backup.md:264–264 | An operator backs up one service | Command: | Awaiting scope verifier |
| BACKUP-051 | specs/stories/backup.md:266–268 | An operator backs up one service | ``` $ sudo opsctl backup crm ``` | Awaiting scope verifier |
| BACKUP-052 | specs/stories/backup.md:270–270 | An operator backs up one service | Output: | Awaiting scope verifier |
| BACKUP-053 | specs/stories/backup.md:272–274 | An operator backs up one service | ``` crm: ok (2026-09-12T14:22:51Z.tar.zst, 1.2 MiB) ``` | Awaiting scope verifier |
| BACKUP-054 | specs/stories/backup.md:276–276 | An operator backs up one service | Exits 0. The line is on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-055 | specs/stories/backup.md:278–278 | An operator backs up one service | Preconditions: | Awaiting scope verifier |
| BACKUP-056 | specs/stories/backup.md:280–280 | An operator backs up one service | - `/opt/crm/` holds an `etc/` or a `state/`. | Awaiting scope verifier |
| BACKUP-057 | specs/stories/backup.md:282–282 | An operator backs up one service | Postconditions: | Awaiting scope verifier |
| BACKUP-058 | specs/stories/backup.md:284–285 | An operator backs up one service | - One object was written, under `crm/` and no other prefix. No other service was read. | Awaiting scope verifier |
| BACKUP-059 | specs/stories/backup.md:289–291 | The host backs up a service it cannot read | One service's failure is a fact about that service, so the run reports it and still backs up everything else — one run tells the operator every problem — and the exit code says the report holds a failure. | Awaiting scope verifier |
| BACKUP-060 | specs/stories/backup.md:293–293 | The host backs up a service it cannot read | Command: | Awaiting scope verifier |
| BACKUP-061 | specs/stories/backup.md:295–297 | The host backs up a service it cannot read | ``` $ sudo opsctl backup; echo "exit $?" ``` | Awaiting scope verifier |
| BACKUP-062 | specs/stories/backup.md:299–299 | The host backs up a service it cannot read | Output: | Awaiting scope verifier |
| BACKUP-063 | specs/stories/backup.md:301–305 | The host backs up a service it cannot read | ``` crm: failed: /opt/crm/state/outbox: permission denied dashboard: ok (2026-09-12T03:00:04Z.tar.zst, 1.1 MiB) exit 1 ``` | Awaiting scope verifier |
| BACKUP-064 | specs/stories/backup.md:307–307 | The host backs up a service it cannot read | Exits 1. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-065 | specs/stories/backup.md:309–309 | The host backs up a service it cannot read | Preconditions: | Awaiting scope verifier |
| BACKUP-066 | specs/stories/backup.md:311–311 | The host backs up a service it cannot read | - `/opt/crm/state/outbox` cannot be read. | Awaiting scope verifier |
| BACKUP-067 | specs/stories/backup.md:313–313 | The host backs up a service it cannot read | Postconditions: | Awaiting scope verifier |
| BACKUP-068 | specs/stories/backup.md:315–318 | The host backs up a service it cannot read | - `dashboard`'s object was written. No object was written for `crm`, and its earlier backups are untouched. - `crm`'s database is unaffected: litestream is still replicating it, and a file the tarball could not read is not a file litestream reads. | Awaiting scope verifier |
| BACKUP-069 | specs/stories/backup.md:322–322 | An operator backs up a service that is not there | Command: | Awaiting scope verifier |
| BACKUP-070 | specs/stories/backup.md:324–326 | An operator backs up a service that is not there | ``` $ sudo opsctl backup gmail ``` | Awaiting scope verifier |
| BACKUP-071 | specs/stories/backup.md:328–328 | An operator backs up a service that is not there | Output: | Awaiting scope verifier |
| BACKUP-072 | specs/stories/backup.md:330–332 | An operator backs up a service that is not there | ``` opsctl: no service 'gmail' ``` | Awaiting scope verifier |
| BACKUP-073 | specs/stories/backup.md:334–334 | An operator backs up a service that is not there | Exits 1. The line is on stderr; stdout is empty. | Awaiting scope verifier |
| BACKUP-074 | specs/stories/backup.md:336–336 | An operator backs up a service that is not there | Preconditions: | Awaiting scope verifier |
| BACKUP-075 | specs/stories/backup.md:338–338 | An operator backs up a service that is not there | - `/opt/gmail/` does not exist, or holds neither an `etc/` nor a `state/`. | Awaiting scope verifier |
| BACKUP-076 | specs/stories/backup.md:340–340 | An operator backs up a service that is not there | Postconditions: | Awaiting scope verifier |
| BACKUP-077 | specs/stories/backup.md:342–342 | An operator backs up a service that is not there | - Nothing has changed. No object was written. | Awaiting scope verifier |
| BACKUP-078 | specs/stories/backup.md:346–346 | An agent backs up a host with nowhere to put it | Command: | Awaiting scope verifier |
| BACKUP-079 | specs/stories/backup.md:348–350 | An agent backs up a host with nowhere to put it | ``` $ sudo opsctl backup ``` | Awaiting scope verifier |
| BACKUP-080 | specs/stories/backup.md:352–352 | An agent backs up a host with nowhere to put it | Output: | Awaiting scope verifier |
| BACKUP-081 | specs/stories/backup.md:354–356 | An agent backs up a host with nowhere to put it | ``` opsctl: backup.s3_uri not set ``` | Awaiting scope verifier |
| BACKUP-082 | specs/stories/backup.md:358–360 | An agent backs up a host with nowhere to put it | Exits 1. The line is on stderr; stdout is empty. With `aws.region` unset the line is `opsctl: aws.region not set`. `opsctl host backup` and `opsctl restore` say the same. | Awaiting scope verifier |
| BACKUP-083 | specs/stories/backup.md:362–362 | An agent backs up a host with nowhere to put it | Preconditions: | Awaiting scope verifier |
| BACKUP-084 | specs/stories/backup.md:364–364 | An agent backs up a host with nowhere to put it | - `backup.s3_uri` is unset or empty. | Awaiting scope verifier |
| BACKUP-085 | specs/stories/backup.md:366–366 | An agent backs up a host with nowhere to put it | Postconditions: | Awaiting scope verifier |
| BACKUP-086 | specs/stories/backup.md:368–368 | An agent backs up a host with nowhere to put it | - Nothing has changed. Nothing was read and no object was written. | Awaiting scope verifier |
| BACKUP-087 | specs/stories/backup.md:372–375 | An operator asks what `host` can do | The host's own configuration is not a service and is not shaped like one: there is one of it, it lives under `/etc/`, and restoring it replaces what the machine is rather than what an app holds. It gets its own noun for that reason. | Awaiting scope verifier |
| BACKUP-088 | specs/stories/backup.md:377–377 | An operator asks what `host` can do | Command: | Awaiting scope verifier |
| BACKUP-089 | specs/stories/backup.md:379–381 | An operator asks what `host` can do | ``` $ opsctl host --help ``` | Awaiting scope verifier |
| BACKUP-090 | specs/stories/backup.md:383–385 | An operator asks what `host` can do | ``` $ opsctl host -h ``` | Awaiting scope verifier |
| BACKUP-091 | specs/stories/backup.md:387–387 | An operator asks what `host` can do | Output: | Awaiting scope verifier |
| BACKUP-092 | specs/stories/backup.md:389–406 | An operator asks what `host` can do | ``` Usage: opsctl host <subcommand>  Back up and restore the host's own configuration: /etc/ikigenba/ and /etc/letsencrypt/, under 'host/' in backup.s3_uri. Nothing under /opt is touched either way -- that is 'opsctl backup' and 'opsctl restore'.  Subcommands: backup    write /etc/ikigenba/ and /etc/letsencrypt/ to S3 restore   replace them with the newest backup  'opsctl init' writes the timer that runs the backup at backup.host_files_seconds.  Configuration keys: aws.region      the region the backup bucket lives in backup.s3_uri   the prefix this host backs up to ``` | Awaiting scope verifier |
| BACKUP-093 | specs/stories/backup.md:408–408 | An operator asks what `host` can do | Exits 0. The text is on stdout; stderr is empty. It prints for any user. | Awaiting scope verifier |
| BACKUP-094 | specs/stories/backup.md:410–410 | An operator asks what `host` can do | Preconditions: | Awaiting scope verifier |
| BACKUP-095 | specs/stories/backup.md:412–412 | An operator asks what `host` can do | - `opsctl` is installed on the host. | Awaiting scope verifier |
| BACKUP-096 | specs/stories/backup.md:414–414 | An operator asks what `host` can do | Postconditions: | Awaiting scope verifier |
| BACKUP-097 | specs/stories/backup.md:416–416 | An operator asks what `host` can do | - Nothing has changed. | Awaiting scope verifier |
| BACKUP-098 | specs/stories/backup.md:420–424 | The host backs up its own configuration | `ikigenba-backup-host.timer` runs this every `backup.host_files_seconds`. The configuration store is reconstructible — `devctl space create` writes every key — but the certificate is not: the CA rate-limits how often it will issue the same names, so a host rebuilt often can find itself unable to get one back. | Awaiting scope verifier |
| BACKUP-099 | specs/stories/backup.md:426–426 | The host backs up its own configuration | Command: | Awaiting scope verifier |
| BACKUP-100 | specs/stories/backup.md:428–430 | The host backs up its own configuration | ``` $ sudo opsctl host backup ``` | Awaiting scope verifier |
| BACKUP-101 | specs/stories/backup.md:432–432 | The host backs up its own configuration | Output: | Awaiting scope verifier |
| BACKUP-102 | specs/stories/backup.md:434–436 | The host backs up its own configuration | ``` host: ok (2026-09-12T03:00:04Z.tar.zst, 48.2 KiB) ``` | Awaiting scope verifier |
| BACKUP-103 | specs/stories/backup.md:438–438 | The host backs up its own configuration | Exits 0. The line is on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-104 | specs/stories/backup.md:440–440 | The host backs up its own configuration | Preconditions: | Awaiting scope verifier |
| BACKUP-105 | specs/stories/backup.md:442–443 | The host backs up its own configuration | - `aws.region` and `backup.s3_uri` are set, and the host's role can write under that prefix. | Awaiting scope verifier |
| BACKUP-106 | specs/stories/backup.md:445–445 | The host backs up its own configuration | Postconditions: | Awaiting scope verifier |
| BACKUP-107 | specs/stories/backup.md:447–450 | The host backs up its own configuration | - `<backup.s3_uri>host/2026-09-12T03:00:04Z.tar.zst` holds `/etc/ikigenba/` and `/etc/letsencrypt/`, with their modes. - Nothing under `/opt/` was read. No earlier object was deleted or overwritten. | Awaiting scope verifier |
| BACKUP-108 | specs/stories/backup.md:454–457 | An operator gives a rebuilt host its certificate back | `/etc/letsencrypt/` comes back rather than being re-issued, which is the point. The configuration store comes back with it, so a host restored this way is configured as the old one was — including any key set by hand that `space create` would not know to write. | Awaiting scope verifier |
| BACKUP-109 | specs/stories/backup.md:459–459 | An operator gives a rebuilt host its certificate back | Command: | Awaiting scope verifier |
| BACKUP-110 | specs/stories/backup.md:461–463 | An operator gives a rebuilt host its certificate back | ``` $ sudo opsctl host restore ``` | Awaiting scope verifier |
| BACKUP-111 | specs/stories/backup.md:465–465 | An operator gives a rebuilt host its certificate back | Output: | Awaiting scope verifier |
| BACKUP-112 | specs/stories/backup.md:467–470 | An operator gives a rebuilt host its certificate back | ``` source: ok (host/2026-09-12T03:00:04Z.tar.zst, 48.2 KiB) files: ok (/etc/ikigenba, /etc/letsencrypt, 31 files) ``` | Awaiting scope verifier |
| BACKUP-113 | specs/stories/backup.md:472–472 | An operator gives a rebuilt host its certificate back | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-114 | specs/stories/backup.md:474–474 | An operator gives a rebuilt host its certificate back | Preconditions: | Awaiting scope verifier |
| BACKUP-115 | specs/stories/backup.md:476–478 | An operator gives a rebuilt host its certificate back | - `aws.region` and `backup.s3_uri` are set, and the host's role can read under that prefix. - `<backup.s3_uri>host/` holds at least one backup. | Awaiting scope verifier |
| BACKUP-116 | specs/stories/backup.md:480–480 | An operator gives a rebuilt host its certificate back | Postconditions: | Awaiting scope verifier |
| BACKUP-117 | specs/stories/backup.md:482–488 | An operator gives a rebuilt host its certificate back | - `/etc/ikigenba/` and `/etc/letsencrypt/` are exactly what the newest backup holds, with their modes. Anything that was there and is not in the backup is gone. - Nothing under `/opt/` was touched, no unit was started or stopped, and nginx was not reloaded. `opsctl init` is what makes the host act on what was just restored. - No object under `<backup.s3_uri>` was written or deleted. | Awaiting scope verifier |
| BACKUP-118 | specs/stories/backup.md:492–495 | An operator asks for a period of zero | A period of `0` — or a key that was never set — is how an account says it wants no backups of that kind. The unit pair is still written, so an operator can run one by hand with `systemctl start`, but the timer is left disabled and stopped and fires nothing. | Awaiting scope verifier |
| BACKUP-119 | specs/stories/backup.md:497–497 | An operator asks for a period of zero | Command: | Awaiting scope verifier |
| BACKUP-120 | specs/stories/backup.md:499–503 | An operator asks for a period of zero | ``` $ sudo opsctl config set backup.service_files_seconds=0 $ sudo opsctl init $ systemctl is-enabled ikigenba-backup-services.timer; echo "exit $?" ``` | Awaiting scope verifier |
| BACKUP-121 | specs/stories/backup.md:505–505 | An operator asks for a period of zero | Output: | Awaiting scope verifier |
| BACKUP-122 | specs/stories/backup.md:507–510 | An operator asks for a period of zero | ``` disabled exit 1 ``` | Awaiting scope verifier |
| BACKUP-123 | specs/stories/backup.md:512–512 | An operator asks for a period of zero | Preconditions: | Awaiting scope verifier |
| BACKUP-124 | specs/stories/backup.md:514–514 | An operator asks for a period of zero | - `opsctl init` reports the host ready. | Awaiting scope verifier |
| BACKUP-125 | specs/stories/backup.md:516–516 | An operator asks for a period of zero | Postconditions: | Awaiting scope verifier |
| BACKUP-126 | specs/stories/backup.md:518–529 | An operator asks for a period of zero | - `ikigenba-backup-host.timer` is enabled and active at its period; `ikigenba-backup-services.timer` is written, disabled, and not running. `ikigenba-renew-certificate.timer` is enabled and active whatever the periods say: there is no key that turns renewal off. - `ikigenba-backup-host.service` runs `opsctl host backup` as root and `ikigenba-backup-services.service` runs `opsctl backup` as root; changing a period is `opsctl config set` followed by `opsctl init`, because a timer is generated from the store and an edit to one would be overwritten by the next `init`. - A space whose periods are all `0` and which has no service declaring a database writes nothing at all, so its prefix stays empty and it can be a restore target but never a source. | Awaiting scope verifier |
| BACKUP-127 | specs/stories/backup.md:533–537 | An operator asks what `retire` can do | `opsctl backup` never stops a unit, so it cannot capture a database: the database is litestream's, and litestream ships on its own clock. A host that is about to be thrown away needs both clocks stopped and read to the end. That is one verb rather than a flag on `backup`, because it is the one command here that interrupts service. | Awaiting scope verifier |
| BACKUP-128 | specs/stories/backup.md:539–539 | An operator asks what `retire` can do | Command: | Awaiting scope verifier |
| BACKUP-129 | specs/stories/backup.md:541–543 | An operator asks what `retire` can do | ``` $ opsctl retire --help ``` | Awaiting scope verifier |
| BACKUP-130 | specs/stories/backup.md:545–547 | An operator asks what `retire` can do | ``` $ opsctl retire -h ``` | Awaiting scope verifier |
| BACKUP-131 | specs/stories/backup.md:549–549 | An operator asks what `retire` can do | Output: | Awaiting scope verifier |
| BACKUP-132 | specs/stories/backup.md:551–565 | An operator asks what `retire` can do | ``` Usage: opsctl retire  Take a host's final backup before it is discarded. Stop every service, then litestream.service so it ships every committed change it holds, then copy every service's files and the host's own configuration to backup.s3_uri exactly as 'opsctl backup' and 'opsctl host backup' would.  Nothing is deleted or disabled. The units are left stopped; a host kept after all comes back with 'systemctl start' or a reboot.  Configuration keys: aws.region      the region the backup bucket lives in backup.s3_uri   the prefix this host backs up to ``` | Awaiting scope verifier |
| BACKUP-133 | specs/stories/backup.md:567–567 | An operator asks what `retire` can do | Exits 0. The text is on stdout; stderr is empty. It prints for any user. | Awaiting scope verifier |
| BACKUP-134 | specs/stories/backup.md:569–569 | An operator asks what `retire` can do | Preconditions: | Awaiting scope verifier |
| BACKUP-135 | specs/stories/backup.md:571–571 | An operator asks what `retire` can do | - `opsctl` is installed on the host. | Awaiting scope verifier |
| BACKUP-136 | specs/stories/backup.md:573–573 | An operator asks what `retire` can do | Postconditions: | Awaiting scope verifier |
| BACKUP-137 | specs/stories/backup.md:575–575 | An operator asks what `retire` can do | - Nothing has changed. | Awaiting scope verifier |
| BACKUP-138 | specs/stories/backup.md:579–582 | An operator takes a host's final backup | `devctl space destroy` runs this over ssh before it terminates a space whose account keeps backups. Each line is one step; the backup lines are the same lines `opsctl backup` and `opsctl host backup` print, and every object of the run carries one timestamp. | Awaiting scope verifier |
| BACKUP-139 | specs/stories/backup.md:584–594 | An operator takes a host's final backup | Stopping litestream is what finishes and closes its WAL stream, and that was verified against 0.5.17 rather than assumed. On stop it has 30 seconds by default (`shutdown-sync-timeout`) to finish the stream: one more sync of the database, the frames not yet shipped uploaded with retries, then the read lock released. The stream is continuous while it runs, so what remains to finish at stop is at most one WAL period of committed changes, normally a small upload. Should the stream not close in time, nothing is lost: every committed change is in the database file on disk, and the tarball this command writes next holds it; only the replica in S3 is behind. The packaged unit sets no stop timeout, so systemd's default 90 seconds covers the close. A second stop signal during the wait skips it. | Awaiting scope verifier |
| BACKUP-140 | specs/stories/backup.md:596–596 | An operator takes a host's final backup | Command: | Awaiting scope verifier |
| BACKUP-141 | specs/stories/backup.md:598–600 | An operator takes a host's final backup | ``` $ sudo opsctl retire ``` | Awaiting scope verifier |
| BACKUP-142 | specs/stories/backup.md:602–602 | An operator takes a host's final backup | Output: | Awaiting scope verifier |
| BACKUP-143 | specs/stories/backup.md:604–610 | An operator takes a host's final backup | ``` services: ok (crm, dashboard stopped) litestream: ok (stopped, crm.db synced) crm: ok (2026-09-12T14:22:51Z.tar.zst, 1.2 MiB) dashboard: ok (2026-09-12T14:22:51Z.tar.zst, 1.1 MiB) host: ok (2026-09-12T14:22:51Z.tar.zst, 48.2 KiB) ``` | Awaiting scope verifier |
| BACKUP-144 | specs/stories/backup.md:612–612 | An operator takes a host's final backup | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-145 | specs/stories/backup.md:614–614 | An operator takes a host's final backup | Preconditions: | Awaiting scope verifier |
| BACKUP-146 | specs/stories/backup.md:616–621 | An operator takes a host's final backup | - `aws.region` and `backup.s3_uri` are set, and the host's role can write under that prefix. - Two services are installed and their units are active. `/opt/crm/etc/manifest.toml` declares a `[database]` at `state/crm.db`; `dashboard`'s declares none. `litestream.service` is active and replicating `crm.db`. | Awaiting scope verifier |
| BACKUP-147 | specs/stories/backup.md:623–623 | An operator takes a host's final backup | Postconditions: | Awaiting scope verifier |
| BACKUP-148 | specs/stories/backup.md:625–637 | An operator takes a host's final backup | - `ikigenba-crm.service`, `ikigenba-dashboard.service`, and `litestream.service` are inactive. Nothing was disabled, masked, or removed; the timers are as they were. - litestream was stopped after the services, so no writer was open when it shut down, and its shutdown sync shipped every WAL frame it held. The replica in S3 holds `crm.db` as of the last committed transaction. - `<backup.s3_uri>crm/2026-09-12T14:22:51Z.tar.zst`, `<backup.s3_uri>dashboard/2026-09-12T14:22:51Z.tar.zst`, and `<backup.s3_uri>host/2026-09-12T14:22:51Z.tar.zst` exist, with the same contents and exclusions the two backup commands write. No earlier object was deleted or overwritten. - A host with no services prints only the `services: ok (none)`, `litestream: ok (stopped)`, and `host:` lines. | Awaiting scope verifier |
| BACKUP-149 | specs/stories/backup.md:641–644 | A host's final backup cannot read one service | As with `opsctl backup`, one service's failure is reported and the rest of the run proceeds, so the operator sees every problem at once. The units stay stopped: the run's job was to stop them, and whoever ran it decides whether the host lives on. | Awaiting scope verifier |
| BACKUP-150 | specs/stories/backup.md:646–646 | A host's final backup cannot read one service | Command: | Awaiting scope verifier |
| BACKUP-151 | specs/stories/backup.md:648–650 | A host's final backup cannot read one service | ``` $ sudo opsctl retire; echo "exit $?" ``` | Awaiting scope verifier |
| BACKUP-152 | specs/stories/backup.md:652–652 | A host's final backup cannot read one service | Output: | Awaiting scope verifier |
| BACKUP-153 | specs/stories/backup.md:654–661 | A host's final backup cannot read one service | ``` services: ok (crm, dashboard stopped) litestream: ok (stopped, crm.db synced) crm: failed: /opt/crm/state/outbox: permission denied dashboard: ok (2026-09-12T14:22:51Z.tar.zst, 1.1 MiB) host: ok (2026-09-12T14:22:51Z.tar.zst, 48.2 KiB) exit 1 ``` | Awaiting scope verifier |
| BACKUP-154 | specs/stories/backup.md:663–663 | A host's final backup cannot read one service | Exits 1. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-155 | specs/stories/backup.md:665–665 | A host's final backup cannot read one service | Preconditions: | Awaiting scope verifier |
| BACKUP-156 | specs/stories/backup.md:667–667 | A host's final backup cannot read one service | - As for the previous story, and `/opt/crm/state/outbox` cannot be read. | Awaiting scope verifier |
| BACKUP-157 | specs/stories/backup.md:669–669 | A host's final backup cannot read one service | Postconditions: | Awaiting scope verifier |
| BACKUP-158 | specs/stories/backup.md:671–673 | A host's final backup cannot read one service | - Every unit is inactive. `dashboard`'s and the host's objects were written; no object was written for `crm`, and its earlier backups are untouched. `crm.db` was shipped in full before the files step ran. | Awaiting scope verifier |
| BACKUP-159 | specs/stories/backup.md:677–677 | An operator asks what `restore` can do | Command: | Awaiting scope verifier |
| BACKUP-160 | specs/stories/backup.md:679–681 | An operator asks what `restore` can do | ``` $ opsctl restore --help ``` | Awaiting scope verifier |
| BACKUP-161 | specs/stories/backup.md:683–685 | An operator asks what `restore` can do | ``` $ opsctl restore -h ``` | Awaiting scope verifier |
| BACKUP-162 | specs/stories/backup.md:687–687 | An operator asks what `restore` can do | Output: | Awaiting scope verifier |
| BACKUP-163 | specs/stories/backup.md:689–717 | An operator asks what `restore` can do | ``` Usage: opsctl restore SERVICE [--at <timestamp>]  Replace /opt/SERVICE/etc/ and /opt/SERVICE/state/ with a backup, and, when SERVICE declares a [database], replace that database with what litestream holds. Without --at both halves are the newest there is. Nothing under bin/ or share/ is touched.  SERVICE's unit is stopped for the restore and started again after it, and so is litestream.service, because the files step deletes the database both of them hold open. A restore is a brief outage. A unit that was already stopped is left stopped, and a failed restore leaves both stopped.  Before litestream.service comes back, /etc/litestream.yml is regenerated from the manifest the restore put in place, so a database restored into a host that never ran SERVICE is replicated from the start line on.  Options: --at <timestamp>    restore the service as it was at this RFC 3339 moment  --at governs both halves: the files come from the newest tarball written at or before that moment, and the database is rebuilt to the moment itself. The two are not the same instant, because the tarball is written on a timer and the database is replicated continuously.  Configuration keys: aws.region      the region the backup bucket lives in backup.s3_uri   the prefix this host backs up to ``` | Awaiting scope verifier |
| BACKUP-164 | specs/stories/backup.md:719–719 | An operator asks what `restore` can do | Exits 0. The text is on stdout; stderr is empty. It prints for any user. | Awaiting scope verifier |
| BACKUP-165 | specs/stories/backup.md:721–721 | An operator asks what `restore` can do | Preconditions: | Awaiting scope verifier |
| BACKUP-166 | specs/stories/backup.md:723–723 | An operator asks what `restore` can do | - `opsctl` is installed on the host. | Awaiting scope verifier |
| BACKUP-167 | specs/stories/backup.md:725–725 | An operator asks what `restore` can do | Postconditions: | Awaiting scope verifier |
| BACKUP-168 | specs/stories/backup.md:727–727 | An operator asks what `restore` can do | - Nothing has changed. | Awaiting scope verifier |
| BACKUP-169 | specs/stories/backup.md:731–737 | An operator puts a service back as it was | The service is running, so the restore stops it, and litestream with it, before the files land; both come back once the database is in place. The `db` line is the database's own newest point, which is later than the tarball's: the tarball is written on a timer and the database is replicated continuously. The `litestream` line is `/etc/litestream.yml` regenerated from the manifest the `files` step just wrote; here it names the same database as before, so the file is unchanged and the line says so. | Awaiting scope verifier |
| BACKUP-170 | specs/stories/backup.md:739–739 | An operator puts a service back as it was | Command: | Awaiting scope verifier |
| BACKUP-171 | specs/stories/backup.md:741–743 | An operator puts a service back as it was | ``` $ sudo opsctl restore crm ``` | Awaiting scope verifier |
| BACKUP-172 | specs/stories/backup.md:745–745 | An operator puts a service back as it was | Output: | Awaiting scope verifier |
| BACKUP-173 | specs/stories/backup.md:747–754 | An operator puts a service back as it was | ``` source: ok (crm/2026-09-12T03:00:04Z.tar.zst, 1.2 MiB) stop: ok (ikigenba-crm.service, litestream.service) files: ok (/opt/crm/etc, /opt/crm/state, 12 files) db: ok (/opt/crm/state/crm.db, newest 2026-09-12T09:07:11Z) litestream: ok (unchanged) start: ok (litestream.service, ikigenba-crm.service) ``` | Awaiting scope verifier |
| BACKUP-174 | specs/stories/backup.md:756–756 | An operator puts a service back as it was | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-175 | specs/stories/backup.md:758–758 | An operator puts a service back as it was | Preconditions: | Awaiting scope verifier |
| BACKUP-176 | specs/stories/backup.md:760–764 | An operator puts a service back as it was | - `aws.region` and `backup.s3_uri` are set, and the host's role can read under that prefix. - `<backup.s3_uri>crm/` holds at least one tarball and litestream has replicated `crm`'s database there. - `ikigenba-crm.service` is active. | Awaiting scope verifier |
| BACKUP-177 | specs/stories/backup.md:766–766 | An operator puts a service back as it was | Postconditions: | Awaiting scope verifier |
| BACKUP-178 | specs/stories/backup.md:768–781 | An operator puts a service back as it was | - `/opt/crm/etc/` and `/opt/crm/state/` are what the newest tarball holds, and then `/opt/crm/state/crm.db` is what litestream restored. Anything that was there and is in neither is gone. - `/opt/crm/bin/` and `/opt/crm/share/` are untouched: the code on the host is the deploy's business, not the restore's. - `ikigenba-crm.service` is active again and `litestream.service` is running, replicating the restored database to `<backup.s3_uri>crm/` as before. The service was down for the length of the restore and for no longer. - `/etc/litestream.yml` is byte for byte as it was: the restored manifest declares the database the installed one did. - No unit was enabled or disabled, and nginx was not reloaded. - The restore itself wrote and deleted no object under `<backup.s3_uri>`. What litestream ships once it is running again is litestream's business, not the restore's. | Awaiting scope verifier |
| BACKUP-179 | specs/stories/backup.md:785–788 | An operator puts a service back as it was at a moment | Something went wrong during the day and the operator knows roughly when. `--at` names the moment, and both halves answer to it: the files are the newest tarball written at or before it, and the database is rebuilt to the moment itself. The `db` line reports the moment asked for rather than a newest. | Awaiting scope verifier |
| BACKUP-180 | specs/stories/backup.md:790–790 | An operator puts a service back as it was at a moment | Command: | Awaiting scope verifier |
| BACKUP-181 | specs/stories/backup.md:792–794 | An operator puts a service back as it was at a moment | ``` $ sudo opsctl restore crm --at 2026-09-11T18:00:00Z ``` | Awaiting scope verifier |
| BACKUP-182 | specs/stories/backup.md:796–796 | An operator puts a service back as it was at a moment | Output: | Awaiting scope verifier |
| BACKUP-183 | specs/stories/backup.md:798–805 | An operator puts a service back as it was at a moment | ``` source: ok (crm/2026-09-11T03:00:02Z.tar.zst, 1.2 MiB) stop: ok (ikigenba-crm.service, litestream.service) files: ok (/opt/crm/etc, /opt/crm/state, 12 files) db: ok (/opt/crm/state/crm.db, at 2026-09-11T18:00:00Z) litestream: ok (unchanged) start: ok (litestream.service, ikigenba-crm.service) ``` | Awaiting scope verifier |
| BACKUP-184 | specs/stories/backup.md:807–807 | An operator puts a service back as it was at a moment | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-185 | specs/stories/backup.md:809–809 | An operator puts a service back as it was at a moment | Preconditions: | Awaiting scope verifier |
| BACKUP-186 | specs/stories/backup.md:811–815 | An operator puts a service back as it was at a moment | - `aws.region` and `backup.s3_uri` are set, and the host's role can read under that prefix. - `<backup.s3_uri>crm/` holds a tarball written at or before `2026-09-11T18:00:00Z`, and litestream's objects there cover that moment. - `ikigenba-crm.service` is active. | Awaiting scope verifier |
| BACKUP-187 | specs/stories/backup.md:817–817 | An operator puts a service back as it was at a moment | Postconditions: | Awaiting scope verifier |
| BACKUP-188 | specs/stories/backup.md:819–827 | An operator puts a service back as it was at a moment | - Everything the ordinary restore's postconditions say, except that `/opt/crm/etc/` and `/opt/crm/state/` are the `2026-09-11T03:00:02Z` tarball's and `/opt/crm/state/crm.db` is the database as it stood at `2026-09-11T18:00:00Z`. - Backups written after that moment were read past and not deleted. The restore is a read: `<backup.s3_uri>crm/` holds what it held before. - litestream replicates forward from the restored database. The history it wrote after the requested moment is litestream's to reconcile, and the restore does not remove it. | Awaiting scope verifier |
| BACKUP-189 | specs/stories/backup.md:831–834 | An operator asks for a moment with no backup before it | The tarball is the half that can be missing: litestream's objects begin when replication began, but the files begin at the first timer run. A moment before the first tarball has nothing to restore the files from, and the restore stops before it stops anything. | Awaiting scope verifier |
| BACKUP-190 | specs/stories/backup.md:836–836 | An operator asks for a moment with no backup before it | Command: | Awaiting scope verifier |
| BACKUP-191 | specs/stories/backup.md:838–840 | An operator asks for a moment with no backup before it | ``` $ sudo opsctl restore crm --at 2026-08-01T00:00:00Z ``` | Awaiting scope verifier |
| BACKUP-192 | specs/stories/backup.md:842–842 | An operator asks for a moment with no backup before it | Output: | Awaiting scope verifier |
| BACKUP-193 | specs/stories/backup.md:844–846 | An operator asks for a moment with no backup before it | ``` opsctl: crm: no backup at or before 2026-08-01T00:00:00Z ``` | Awaiting scope verifier |
| BACKUP-194 | specs/stories/backup.md:848–848 | An operator asks for a moment with no backup before it | Exits 1. The line is on stderr; stdout is empty. | Awaiting scope verifier |
| BACKUP-195 | specs/stories/backup.md:850–850 | An operator asks for a moment with no backup before it | Preconditions: | Awaiting scope verifier |
| BACKUP-196 | specs/stories/backup.md:852–853 | An operator asks for a moment with no backup before it | - `<backup.s3_uri>crm/` holds tarballs, and the oldest is newer than `2026-08-01T00:00:00Z`. | Awaiting scope verifier |
| BACKUP-197 | specs/stories/backup.md:855–855 | An operator asks for a moment with no backup before it | Postconditions: | Awaiting scope verifier |
| BACKUP-198 | specs/stories/backup.md:857–858 | An operator asks for a moment with no backup before it | - Nothing has changed. No unit was stopped and nothing under `/opt/crm/` was read or written. | Awaiting scope verifier |
| BACKUP-199 | specs/stories/backup.md:862–868 | An operator puts back a service that keeps no database | With no `[database]` in the manifest there is nothing for litestream to have replicated, and the whole of `state/` was in the tarball. The `db` line is absent rather than reported as skipped: a line claiming a database was handled would be a lie about what is on disk. litestream is absent from the `stop:` and `start:` lines for the same reason, and there is no `litestream` line at all: this service gave it nothing to hold, so the restore has no cause to take it down, no cause to regenerate its configuration, and no cause to say it did. | Awaiting scope verifier |
| BACKUP-200 | specs/stories/backup.md:870–870 | An operator puts back a service that keeps no database | Command: | Awaiting scope verifier |
| BACKUP-201 | specs/stories/backup.md:872–874 | An operator puts back a service that keeps no database | ``` $ sudo opsctl restore dashboard ``` | Awaiting scope verifier |
| BACKUP-202 | specs/stories/backup.md:876–876 | An operator puts back a service that keeps no database | Output: | Awaiting scope verifier |
| BACKUP-203 | specs/stories/backup.md:878–883 | An operator puts back a service that keeps no database | ``` source: ok (dashboard/2026-09-12T03:00:04Z.tar.zst, 1.1 MiB) stop: ok (ikigenba-dashboard.service) files: ok (/opt/dashboard/etc, /opt/dashboard/state, 40 files) start: ok (ikigenba-dashboard.service) ``` | Awaiting scope verifier |
| BACKUP-204 | specs/stories/backup.md:885–885 | An operator puts back a service that keeps no database | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-205 | specs/stories/backup.md:887–887 | An operator puts back a service that keeps no database | Preconditions: | Awaiting scope verifier |
| BACKUP-206 | specs/stories/backup.md:889–890 | An operator puts back a service that keeps no database | - `/opt/dashboard/etc/manifest.toml` declares no `[database]`. - `ikigenba-dashboard.service` is active. | Awaiting scope verifier |
| BACKUP-207 | specs/stories/backup.md:892–892 | An operator puts back a service that keeps no database | Postconditions: | Awaiting scope verifier |
| BACKUP-208 | specs/stories/backup.md:894–898 | An operator puts back a service that keeps no database | - `/opt/dashboard/state/` is exactly what the tarball holds, and is the whole of it. No litestream call was made, `/etc/litestream.yml` was not regenerated, and `litestream.service` was left running throughout, still replicating every other service's database. - `ikigenba-dashboard.service` is active again. | Awaiting scope verifier |
| BACKUP-209 | specs/stories/backup.md:902–906 | An operator restores a service that was already stopped | A restore puts a service's data back; it does not decide whether the service should be running. An operator who stopped `crm` on purpose — mid-incident, or to hold it down while they look at it — gets it back stopped, and the report says so rather than leaving them to find out. litestream is a different matter: the restore took it down for its own reasons, so the restore puts it back. | Awaiting scope verifier |
| BACKUP-210 | specs/stories/backup.md:908–908 | An operator restores a service that was already stopped | Command: | Awaiting scope verifier |
| BACKUP-211 | specs/stories/backup.md:910–912 | An operator restores a service that was already stopped | ``` $ sudo opsctl restore crm ``` | Awaiting scope verifier |
| BACKUP-212 | specs/stories/backup.md:914–914 | An operator restores a service that was already stopped | Output: | Awaiting scope verifier |
| BACKUP-213 | specs/stories/backup.md:916–923 | An operator restores a service that was already stopped | ``` source: ok (crm/2026-09-12T03:00:04Z.tar.zst, 1.2 MiB) stop: ok (litestream.service, ikigenba-crm.service already inactive) files: ok (/opt/crm/etc, /opt/crm/state, 12 files) db: ok (/opt/crm/state/crm.db, newest 2026-09-12T09:07:11Z) litestream: ok (unchanged) start: ok (litestream.service, ikigenba-crm.service left inactive) ``` | Awaiting scope verifier |
| BACKUP-214 | specs/stories/backup.md:925–925 | An operator restores a service that was already stopped | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-215 | specs/stories/backup.md:927–927 | An operator restores a service that was already stopped | Preconditions: | Awaiting scope verifier |
| BACKUP-216 | specs/stories/backup.md:929–929 | An operator restores a service that was already stopped | - `ikigenba-crm.service` is installed and is not active. | Awaiting scope verifier |
| BACKUP-217 | specs/stories/backup.md:931–931 | An operator restores a service that was already stopped | Postconditions: | Awaiting scope verifier |
| BACKUP-218 | specs/stories/backup.md:933–936 | An operator restores a service that was already stopped | - Everything the ordinary restore's postconditions say, and: the unit is still inactive and was never started. `litestream.service` is running again. - No unit was enabled or disabled. Whether `crm` comes up at the next boot is what it was before the restore. | Awaiting scope verifier |
| BACKUP-219 | specs/stories/backup.md:940–940 | An operator restores a service with no backups | Command: | Awaiting scope verifier |
| BACKUP-220 | specs/stories/backup.md:942–944 | An operator restores a service with no backups | ``` $ sudo opsctl restore gmail ``` | Awaiting scope verifier |
| BACKUP-221 | specs/stories/backup.md:946–946 | An operator restores a service with no backups | Output: | Awaiting scope verifier |
| BACKUP-222 | specs/stories/backup.md:948–950 | An operator restores a service with no backups | ``` opsctl: no backups for gmail under s3://sbx-ikigenba-dev-602773793009/foo.sbx.ikigenba.dev/ ``` | Awaiting scope verifier |
| BACKUP-223 | specs/stories/backup.md:952–952 | An operator restores a service with no backups | Exits 1. The line is on stderr; stdout is empty. | Awaiting scope verifier |
| BACKUP-224 | specs/stories/backup.md:954–954 | An operator restores a service with no backups | Preconditions: | Awaiting scope verifier |
| BACKUP-225 | specs/stories/backup.md:956–956 | An operator restores a service with no backups | - `<backup.s3_uri>gmail/` holds no object. | Awaiting scope verifier |
| BACKUP-226 | specs/stories/backup.md:958–958 | An operator restores a service with no backups | Postconditions: | Awaiting scope verifier |
| BACKUP-227 | specs/stories/backup.md:960–960 | An operator restores a service with no backups | - Nothing has changed. `/opt/gmail/` was neither created nor touched. | Awaiting scope verifier |
| BACKUP-228 | specs/stories/backup.md:964–968 | A restore finds files but no database | The tarball and the database reach S3 by two different paths, so one can be there without the other — a prefix staged by hand, a service whose litestream replication never started, a `[database]` added to a manifest after the last backup. The files are restored before the database is looked for, so the steps that ran say so and the restore stops where it failed. | Awaiting scope verifier |
| BACKUP-229 | specs/stories/backup.md:970–977 | A restore finds files but no database | Nothing is started again. `crm` would come up on the tarball's files with no database under them, which is a worse state than being down, and litestream would be asked to replicate a database that is not there. Leaving both stopped is what makes the failure visible and safe to re-run, and the diagnostic says so, because an operator who reads only the error still has to know two units are down. The `litestream` step comes after `db`, so `/etc/litestream.yml` was not regenerated either: a database that is not on disk is not written into the configuration. | Awaiting scope verifier |
| BACKUP-230 | specs/stories/backup.md:979–979 | A restore finds files but no database | Command: | Awaiting scope verifier |
| BACKUP-231 | specs/stories/backup.md:981–983 | A restore finds files but no database | ``` $ sudo opsctl restore crm; echo "exit $?" ``` | Awaiting scope verifier |
| BACKUP-232 | specs/stories/backup.md:985–985 | A restore finds files but no database | Output: | Awaiting scope verifier |
| BACKUP-233 | specs/stories/backup.md:987–995 | A restore finds files but no database | ``` source: ok (crm/2026-09-12T03:00:04Z.tar.zst, 1.2 MiB) stop: ok (ikigenba-crm.service, litestream.service) files: ok (/opt/crm/etc, /opt/crm/state, 12 files) opsctl: crm: litestream restore: no snapshot under the prefix  ikigenba-crm.service and litestream.service were left stopped exit 1 ``` | Awaiting scope verifier |
| BACKUP-234 | specs/stories/backup.md:997–997 | A restore finds files but no database | Exits 1. The `ok` lines are on stdout; the diagnostic is on stderr. | Awaiting scope verifier |
| BACKUP-235 | specs/stories/backup.md:999–999 | A restore finds files but no database | Preconditions: | Awaiting scope verifier |
| BACKUP-236 | specs/stories/backup.md:1001–1004 | A restore finds files but no database | - `<backup.s3_uri>crm/` holds a tarball, and litestream has replicated nothing for `crm`. - `/opt/crm/etc/manifest.toml` in that tarball declares a `[database]`. - `ikigenba-crm.service` is active. | Awaiting scope verifier |
| BACKUP-237 | specs/stories/backup.md:1006–1006 | A restore finds files but no database | Postconditions: | Awaiting scope verifier |
| BACKUP-238 | specs/stories/backup.md:1008–1016 | A restore finds files but no database | - `/opt/crm/etc/` and `/opt/crm/state/` are the tarball's, and there is no database at `/opt/crm/state/crm.db`: the tarball never held one. - `ikigenba-crm.service` and `litestream.service` are both stopped, and neither was enabled or disabled. No database on this host is being replicated until one of them is started. `/etc/litestream.yml` is as it was before the restore. - Nothing was rolled back. The host is left where an operator can look at it, and re-running the restore once the database objects are in place finishes the job and starts both. | Awaiting scope verifier |
| BACKUP-239 | specs/stories/backup.md:1020–1025 | An operator restores into a host that has never run the service | A restore is how a fresh host is given another host's data, so the service need not be installed first. What comes back is data and only data: the host has an `etc/` and a `state/` for a service with no binary until a deploy brings one. The database is replicated from the moment litestream comes back: the `litestream` step read the manifest the `files` step wrote and put the database into `/etc/litestream.yml`, which had not named it before. | Awaiting scope verifier |
| BACKUP-240 | specs/stories/backup.md:1027–1027 | An operator restores into a host that has never run the service | Command: | Awaiting scope verifier |
| BACKUP-241 | specs/stories/backup.md:1029–1031 | An operator restores into a host that has never run the service | ``` $ sudo opsctl restore crm ``` | Awaiting scope verifier |
| BACKUP-242 | specs/stories/backup.md:1033–1033 | An operator restores into a host that has never run the service | Output: | Awaiting scope verifier |
| BACKUP-243 | specs/stories/backup.md:1035–1042 | An operator restores into a host that has never run the service | ``` source: ok (crm/2026-09-12T03:00:04Z.tar.zst, 1.2 MiB) stop: ok (litestream.service, no ikigenba-crm.service) files: ok (/opt/crm/etc, /opt/crm/state, 12 files) db: ok (/opt/crm/state/crm.db, newest 2026-09-12T09:07:11Z) litestream: ok (state/crm.db) start: ok (litestream.service) ``` | Awaiting scope verifier |
| BACKUP-244 | specs/stories/backup.md:1044–1044 | An operator restores into a host that has never run the service | Exits 0. The lines are on stdout; stderr is empty. | Awaiting scope verifier |
| BACKUP-245 | specs/stories/backup.md:1046–1046 | An operator restores into a host that has never run the service | Preconditions: | Awaiting scope verifier |
| BACKUP-246 | specs/stories/backup.md:1048–1048 | An operator restores into a host that has never run the service | - `/opt/crm/` does not exist and no `ikigenba-crm.service` is installed. | Awaiting scope verifier |
| BACKUP-247 | specs/stories/backup.md:1050–1050 | An operator restores into a host that has never run the service | Postconditions: | Awaiting scope verifier |
| BACKUP-248 | specs/stories/backup.md:1052–1066 | An operator restores into a host that has never run the service | - `/opt/crm/etc/` and `/opt/crm/state/` hold the backup's data. `/opt/crm/` was created with mode `0755` and holds nothing else. - The manifest the restore just wrote is what said the service declares a database, so the `db` line is there even though nothing was installed. - There is no unit to stop and none was written: a restore installs data, not a service. `litestream.service` was stopped and started all the same, because `state/` was replaced underneath it. - `/etc/litestream.yml` names `/opt/crm/state/crm.db`, replicating to `<backup.s3_uri>crm/`, and `litestream.service` is running under it. The restored database is being backed up from the `start` line on; no `opsctl init` is needed to make that so. The deploy that later brings the binary regenerates the file again and finds it unchanged. - `opsctl status` shows `crm - - wal` until an app is deployed over it: no binary to ask a version of and no unit to ask a state of, but a restored database whose journal mode it can read. | Awaiting scope verifier |
| BACKUP-249 | specs/stories/backup.md:1070–1070 | An operator runs restore with no service, or more than one | Command: | Awaiting scope verifier |
| BACKUP-250 | specs/stories/backup.md:1072–1074 | An operator runs restore with no service, or more than one | ``` $ sudo opsctl restore ``` | Awaiting scope verifier |
| BACKUP-251 | specs/stories/backup.md:1076–1076 | An operator runs restore with no service, or more than one | Output: | Awaiting scope verifier |
| BACKUP-252 | specs/stories/backup.md:1078–1082 | An operator runs restore with no service, or more than one | ``` opsctl: restore needs SERVICE  see 'opsctl restore --help' for usage ``` | Awaiting scope verifier |
| BACKUP-253 | specs/stories/backup.md:1084–1086 | An operator runs restore with no service, or more than one | Exits 2. The text is on stderr; stdout is empty. More than one operand gives `opsctl: restore takes one SERVICE`, and an `--at` that is not an RFC 3339 timestamp gives `opsctl: --at takes an RFC 3339 timestamp`. | Awaiting scope verifier |
| BACKUP-254 | specs/stories/backup.md:1088–1088 | An operator runs restore with no service, or more than one | Preconditions: | Awaiting scope verifier |
| BACKUP-255 | specs/stories/backup.md:1090–1090 | An operator runs restore with no service, or more than one | - `opsctl` is running as root. | Awaiting scope verifier |
| BACKUP-256 | specs/stories/backup.md:1092–1092 | An operator runs restore with no service, or more than one | Postconditions: | Awaiting scope verifier |
| BACKUP-257 | specs/stories/backup.md:1094–1094 | An operator runs restore with no service, or more than one | - Nothing has changed. | Awaiting scope verifier |
| BACKUP-258 | specs/stories/backup.md:1098–1098 | An operator runs `host` with no subcommand, or one that does not exist | Command: | Awaiting scope verifier |
| BACKUP-259 | specs/stories/backup.md:1100–1102 | An operator runs `host` with no subcommand, or one that does not exist | ``` $ sudo opsctl host ``` | Awaiting scope verifier |
| BACKUP-260 | specs/stories/backup.md:1104–1104 | An operator runs `host` with no subcommand, or one that does not exist | Output: | Awaiting scope verifier |
| BACKUP-261 | specs/stories/backup.md:1106–1110 | An operator runs `host` with no subcommand, or one that does not exist | ``` opsctl: no host subcommand given  see 'opsctl host --help' for usage ``` | Awaiting scope verifier |
| BACKUP-262 | specs/stories/backup.md:1112–1112 | An operator runs `host` with no subcommand, or one that does not exist | Command: | Awaiting scope verifier |
| BACKUP-263 | specs/stories/backup.md:1114–1116 | An operator runs `host` with no subcommand, or one that does not exist | ``` $ sudo opsctl host status ``` | Awaiting scope verifier |
| BACKUP-264 | specs/stories/backup.md:1118–1118 | An operator runs `host` with no subcommand, or one that does not exist | Output: | Awaiting scope verifier |
| BACKUP-265 | specs/stories/backup.md:1120–1124 | An operator runs `host` with no subcommand, or one that does not exist | ``` opsctl: unknown host subcommand 'status'  see 'opsctl host --help' for usage ``` | Awaiting scope verifier |
| BACKUP-266 | specs/stories/backup.md:1126–1126 | An operator runs `host` with no subcommand, or one that does not exist | Both exit 2. The text is on stderr; stdout is empty. | Awaiting scope verifier |
| BACKUP-267 | specs/stories/backup.md:1128–1128 | An operator runs `host` with no subcommand, or one that does not exist | Preconditions: | Awaiting scope verifier |
| BACKUP-268 | specs/stories/backup.md:1130–1130 | An operator runs `host` with no subcommand, or one that does not exist | - `opsctl` is running as root. | Awaiting scope verifier |
| BACKUP-269 | specs/stories/backup.md:1132–1132 | An operator runs `host` with no subcommand, or one that does not exist | Postconditions: | Awaiting scope verifier |
| BACKUP-270 | specs/stories/backup.md:1134–1134 | An operator runs `host` with no subcommand, or one that does not exist | - Nothing has changed. | Awaiting scope verifier |
