# Final init integration verification

Fresh read-only verifier `/root/early/reverify_init` was explicitly assigned
D05↔D06/D07/D11/D12 integration and the C.02 ten-key consumer task. It read final
corrections and complete source stories. No edits, probes, tests, gates or commits.

## Result and per-criterion verdicts

Complete ten-key positive database/WAL-period configuration passes. D07
R-FRFQ-VNTM now rejects missing email before execution, including from init.
The exact 28-label matrix in early-init-verification-1.md remains current:
**19 P / 8 U / 1 B**. The previous headline 18/9/1 was arithmetic error.
U rows are init/intro.command, init/intro.refresh, init/ready.command-output,
init/ready.post.files-units, init/retry.command-pre, init/retry.output-post,
backup/what-is-backed-up.init-steps, backup/period-zero.command-post. B is
init/intro.regeneration, assigned to root D14 integration. Every other row passes.

Six init headings: help, not-ready, arguments, corrupt fully verified (4);
ready and retry partial (2), passing in complete positive configuration. Their
16 criteria are 12 P/4 U. Opening/addition rows counted separately.

## Configuration integration closed

C.02.intro passes: the exact ten configured-host keys supply every setup input.
DNS consumes provider/zones; D05 host.name; D07 email; D11 region/prefix/database
and WAL periods; D12 the file periods. No eleventh key needed in this domain.
C.00.intro passes: D13 R-YCP3-2YZF includes /etc/ikigenba/, hence config.json,
in host archive. “First thing backed up” does not impose separate scheduling.
Consequently all eleven config headings pass. The expanded bootstrap/config
matrix now totals 123 P, zero X, four N/A among 127 cells.

## Boundary and task evidence

D05 R-LTT7-37TO orders cert.Obtain, nginx.Apply, backup.SetupReplication,
backup.SetupTimers. Signatures resolve D07 R-YCKR-XBRL, D06 R-575I-X9KF,
D11 R-JLOW-YJOY, D12 R-FJVO-5X9J. D01 owns CLI/dependencies/host.Env, D03 Store;
dependency directions consistent. Certificate can issue before nginx active;
D06 owns publishing/testing/reload and restoration on test rejection. D11
regenerates current databases and enables shared service. D12 owns both file
backup pairs, zero file periods, mandatory renewal, package-timer masking.

Full preflight report remains stdout; first setup error terminates sequence,
prior effects remain, D02 handles captured diagnostic detail. Setup success adds
no output. Four independent PATH checks, independent provider/zones validation,
one provider opening with retained resolver, independent host membership,
both wildcard lookups, exact report order, skipped dependency lines, corruption
handling and read-only preflight pass. Help decodes byte-identically to source.
Four retained HEAD requirements unchanged, sixteen added, ten removed.

## Remaining ownership and issues

U rows link ../issues/backup-replication-periods.md: zero/unset database/WAL and
absent region/prefix unresolved; all-zero/no-database success unestablished.
Routine malformed validation remains feasible backup-owner work, not user policy.

Root owns B reconciliation: D09 R-P0DV-8KCB covers install regeneration, D14
R-G2KD-J7PA ordinary DB restore regeneration/start without enablement. D14
R-G9VR-TU5G forbids nginx regeneration despite init intro broadly covering both
files and detailed backup story forbidding only reload. Root/apps_backup notified.

External observations are separate before-check dependencies in central ledger.
