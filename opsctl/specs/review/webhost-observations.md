# Webhost external observations

Read-only observations at 2026-09-15T03:28:36Z (2026-09-14 local date) over `ssh dev` (the designated live host). Raw public help output is retained in [webhost-live-help.txt](webhost-live-help.txt). Commands: `nginx -V`, `nginx -h`, `systemctl --help`, and `certbot --help all`; both output streams of nginx help/version were collected. No certificate keys, account credentials, or private configuration were read. No issuance, renewal, nginx test, reload, or live writes were performed.

- nginx reports TLS SNI support and exposes `-t` as configuration test and exit. This verifies invocation grammar, not acceptance of the proposed generated configuration or successful reload without interruption.
- systemctl help exposes `reload UNIT...` and related unit commands.
- certbot help exposes `certonly`, `renew`, repeated `-d`, `--cert-name`, noninteractive mode, email, agreement flag, manual authenticator and DNS preference, authentication and cleanup hooks, and deploy hooks. `--keep-until-expiring` preserves matching certificates until due. Help states renewal reuses previous plugins/options and deploy hooks run upon successful issuance. This is observed tool interface evidence; a successful real issuance, saved renewal configuration, DNS cleanup, and no-op renewal remain unobserved.
- The live `/etc/nginx/conf.d/ikigenba.conf` is absent. `systemctl show nginx.service -p LoadState -p ActiveState -p ExecReload` reported loaded, inactive, and reload command `/usr/sbin/nginx -s reload`; therefore no TLS-frame, glob-include, or active-service continuity was observed. Root's environment observations additionally record available tool versions.

Pending before check-spec: acceptance of the generated nginx frame (including TLS handshake rejection and absent include glob), reload continuity/rollback behavior, successful wildcard plus apex DNS-01 issuance and retained hooks, challenge cleanup, due-only renewal behavior, and renewal service/timer behavior. Documentation/help establishes grammar but does not claim these live outcomes happened.
