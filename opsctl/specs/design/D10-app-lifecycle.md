# D10-app-lifecycle

App removal preserves data, restart reuses the installed configuration, and
status reports host facts. The CLI composes routing and replication around
apps-owned lifecycle operations.

Every app runs as a socket unit and a service unit (D09). Uninstall stops the
socket before the service, so no request starts the service again while it
is taken down, and removes both. Restart restarts the service alone; the
socket keeps listening, so no request is refused. Status reports both units:
the socket's field reads `disabled` when systemd reports the socket unit
disabled.

Disabling is the operator's way to keep an installed app down without
uninstalling it: both units are stopped and disabled, and the app's names
answer 503 (D06). Nothing but systemd's enablement records the state, and
`Disabled` is the one predicate every consumer asks. A disabled app stays
disabled through every operation — install, init, restart, restore, retire —
and only `enable` enables it; uninstall ends the state by removing the units.
The authenticator, `auth`, is never disabled: every other app is checked
against it. Disable and enable are idempotent, and they regenerate nginx
through D06's `Update`, which publishes and reloads only when the rendered
file differs from the one in place. Install and restart share a service-completion
report, while status includes the database journal mode in its host report.

Uninstall's nginx line names every name the app stopped answering at: its own,
the host's when it was the default, and the root domain's apex when
`host.apex` names it. `host.apex` itself is read before anything is stopped or
removed, refused there when the host name has no parent domain, and otherwise
passed through, never changed, so the apex name falls back to the 404 block
until the app returns.
The parameter `/<host.name>/<app>` is devctl's and is never touched. The host
name uninstall checks, passes to nginx, and prints is `host.NormalizeName` of
the stored `host.name` (D01).

## REQUIREMENTS

- R-V0XX-G6S5: Package `internal/apps` MUST own app removal, restart, disabling, enabling, and host-local status, using the D08 service model and D01 host boundary; `internal/cli` MUST own their grammar, rendering, and orchestration with nginx and backup, without adding an apps dependency on those packages.
- R-ES81-UK2R: Package `internal/apps` MUST export `UninstallHooks` as a struct with exactly `Report func(step, detail string, success bool) error` and `Configure func(context.Context, Manifest) error` fields.
- R-LONI-RKQN: Package `internal/apps` MUST export `Uninstall(ctx context.Context, env host.Env, app string, hooks UninstallHooks) error`.
- R-V25T-TYIU: Package `internal/apps` MUST export `Restart(ctx context.Context, env host.Env, app string) (ServiceReport, error)`.
- R-V3DQ-7Q9J: Package `internal/apps` MUST export `ServiceReport` as a struct with exactly `Name string`, `Version string`, and `State string` fields.
- R-V4LM-LI08: Package `internal/apps` MUST export `StatusRow` as a struct with exactly `Name string`, `Version string`, `State string`, `Socket string`, and `JournalMode string` fields.
- R-LSB7-WVYQ: Package `internal/apps` MUST export `Status(ctx context.Context, env host.Env) ([]StatusRow, error)`.
- R-LTJ4-ANPF: Package `internal/apps` MUST export `LifecycleError` as a struct with exactly `Code int`, `Message string`, and `Cause error` fields; `(*LifecycleError).Error() string` MUST return `Message`, and `(*LifecycleError).Unwrap() error` MUST return `Cause`.
- R-V5TI-Z9QX: Package `internal/apps` MUST export `LifecycleHooks` as a struct with exactly `Report func(step, detail string, success bool) error` and `Configure func(context.Context, Manifest) error` fields.
- R-V71F-D1HM: Package `internal/apps` MUST export `Disable(ctx context.Context, env host.Env, app string, hooks LifecycleHooks) error` and `Enable(ctx context.Context, env host.Env, app string, hooks LifecycleHooks) error`.
- R-V89B-QT8B: Package `internal/apps` MUST export `Disabled(ctx context.Context, env host.Env, app string) (bool, error)`.
- R-V9H8-4KZ0: `opsctl uninstall --help` and `opsctl uninstall -h` MUST, for any effective user id, perform no host-file access, subprocess execution or cloud call, change nothing, exit 0 with empty stderr, and write exactly `"Usage: opsctl uninstall APP\n\nTake APP off the host: stop ikigenba-APP.socket and ikigenba-APP.service,\nsocket first so no request starts the service again, disable both, remove\nboth units (which ends a disabled APP's disabled state: a later install is a\nfirst install and comes up enabled), then remove /opt/APP/bin/, etc/, share/, and cache/. /opt/APP/state/ is kept\nuntouched, so APP is still a service the host backs up, and a later install\nlands over its data the way an install over a restore does. Removing state/ is\na decision made by hand, never here.\n\nThe nginx configuration and /etc/litestream.yml are regenerated from every app\nleft on the host, so APP's name stops answering, and a database APP declared\nstops being replicated once litestream has shipped what it holds.\n\nThe parameter /<host.name>/APP is not touched: it is devctl's.\n\nConfiguration keys:\n  host.name  the fully-qualified name this host answers at\n"` to stdout, where `\n` denotes one LF byte.
- R-VAP4-ICPP: `opsctl restart --help` and `opsctl restart -h` MUST, for any effective user id, perform no host-file access, subprocess execution or cloud call, change nothing, exit 0 with empty stderr, and write exactly `"Usage: opsctl restart APP\n\nRestart ikigenba-APP.service and report the service as the last line of\n'opsctl install' does. The socket is never restarted: it keeps listening, so\nrequests that arrive during the restart wait and are answered by the new\nprocess. Nothing on disk changes: the binary, the environment file, and the\nunits are what the last install wrote, so a secret pushed since then is not\npicked up here, nor a timing setting changed since then ('opsctl init'\napplies those). A service that is inactive or failed is started, and so is\nits socket if it was stopped. A disabled app is not started: it stays\ndisabled until 'opsctl enable'.\n"` to stdout, where `\n` denotes one LF byte.
- R-VBX0-W4GE: `opsctl status --help` and `opsctl status -h` MUST, for any effective user id, perform no host-file access, subprocess execution or cloud call, change nothing, exit 0 with empty stderr, and write exactly `"Usage: opsctl status\n\nPrint one line per service on this host, in name order: its name, the version\nits own binary reports, the state of its service unit, the state of its socket\nunit, and the journal mode of the database its manifest declares. A service is\nany /opt/<name>/ with an etc/ or state/ directory; '-' means opsctl could not\nask, or there was nothing to ask.\n\nA service that is inactive behind an active socket is idle, not down: its\nsocket starts it again when the next request arrives. The socket's field reads\n'disabled' when its unit is disabled, as 'opsctl disable' leaves it: neither\nunit starts until 'opsctl enable'.\n\nA declared database must stay in WAL mode: litestream cannot replicate one in\nany other mode, so a service reporting anything but 'wal' is a service whose\ndata is not reaching S3.\n\nThe exit code is 0 whatever the report says. A failed unit and an unreplicable\ndatabase are facts about the host, not failures of this command.\n"` to stdout, where `\n` denotes one LF byte.
- R-VD4X-9W73: `opsctl disable --help` and `opsctl disable -h` MUST, for any effective user id, perform no host-file access, subprocess execution or cloud call, change nothing, exit 0 with empty stderr, and write exactly `"Usage: opsctl disable APP\n\nStop ikigenba-APP.socket and ikigenba-APP.service, socket first so no request\nstarts the service again, and disable both, so neither starts at boot or on a\nrequest. The nginx configuration is then regenerated, so APP's names answer\n503 until it is enabled. Nothing on disk under /opt/APP/ changes.\n'opsctl enable APP' undoes it.\n\nauth, the authenticator every other app is checked against, is never\ndisabled.\n\nConfiguration keys:\n  host.name  the fully-qualified name this host answers at\n  host.apex  the app that answers at the parent of host.name; unset means none\n"` to stdout, where `\n` denotes one LF byte.
- R-VECT-NNXS: `opsctl enable --help` and `opsctl enable -h` MUST, for any effective user id, perform no host-file access, subprocess execution or cloud call, change nothing, exit 0 with empty stderr, and write exactly `"Usage: opsctl enable APP\n\nEnable ikigenba-APP.socket and ikigenba-APP.service and start the socket,\nregenerate the nginx configuration so APP's names reach it again, then start\nthe service and report it as the last line of 'opsctl install' does. Nothing\non disk under /opt/APP/ changes.\n\nConfiguration keys:\n  host.name  the fully-qualified name this host answers at\n  host.apex  the app that answers at the parent of host.name; unset means none\n"` to stdout, where `\n` denotes one LF byte.
- R-VFKQ-1FOH: The uninstall, restart, disable, and enable actions MUST each accept exactly one APP; before host access, zero or multiple operands MUST exit 2 with empty stdout and respectively `opsctl: <command> needs APP` or `opsctl: <command> takes one APP`, followed by one empty line and `see 'opsctl <command> --help' for usage`, with every line LF-terminated; status with any operand MUST use the same formatting with `opsctl: status takes no arguments` and its own usage hint.
- R-VGSM-F7F6: `Uninstall`, `Restart`, `Disable`, and `Enable` MUST reject names not accepted by `ValidateName` before host access with a `LifecycleError` of Code 2 and Message `'<name>' is not a usable app name`; a missing `/opt/<app>/` MUST return Code 1 and Message `no service '<app>'`, and an existing directory without `bin/<app>` MUST return Code 1 and Message `<app> is not installed`, without mutation, subprocess execution, or successful reports.
- R-XRPS-FLUE: Before removal, `Uninstall` MUST require non-nil hooks, a readable valid installed manifest matching the app, and both `/etc/systemd/system/ikigenba-<app>.socket` and `/etc/systemd/system/ikigenba-<app>.service`, reporting a missing unit file as `<app> is not installed`; it MUST retain the manifest for the Configure callback and reject missing or invalid prerequisites without mutation; the CLI MUST also read `host.name` from the `config.Store` and take `host.NormalizeName` of that value as the host name, requiring it to be nonempty before invoking removal and reporting `host.name not set` when the key is absent or the value is empty after normalisation, and MUST read `host.apex` from the same `config.Store` before invoking removal, treating an unset key as the empty string and failing before removal with a D02 diagnostic on any other read error; uninstall MUST NOT set, clear, or change `host.apex`.
- R-VI0I-SZ5V: `Uninstall` MUST stop `ikigenba-<app>.socket` and then `ikigenba-<app>.service`, the socket first so no request starts the service again, and disable both, before removing any app files or unit file; units already inactive or failed, including those of a disabled app, MUST still be disabled and removed. The `stop` detail MUST be `ikigenba-<app>.socket, ikigenba-<app>.service stopped, disabled` when either unit was active at invocation and `ikigenba-<app>.socket, ikigenba-<app>.service already inactive, disabled` otherwise.
- R-VJ8F-6QWK: After successful stop and disable, `Uninstall` MUST remove `/etc/systemd/system/ikigenba-<app>.socket` and `/etc/systemd/system/ikigenba-<app>.service`, execute `systemctl daemon-reload`, and report `unit` with detail `removed ikigenba-<app>.socket, ikigenba-<app>.service` only after all succeed; removing both units ends a disabled app's disabled state, so a later install of the app is a first install.
- R-M4I7-QLDO: After unit removal, `Uninstall` MUST remove `/opt/<app>/bin/`, `etc/`, `share/`, and `cache/`, treating an absent optional directory as already removed, and report `files` with detail `removed /opt/<app>/bin, etc, share, cache; kept state`; it MUST leave `/opt/<app>/state/` and all its descendants untouched, neither reading nor writing their contents or metadata directly, and MUST not follow symlinks in removed paths into preserved data or outside the app tree.
- R-X6R5-769H: After files removal, `Uninstall` MUST invoke `hooks.Configure` exactly once with the removed manifest; the CLI callback MUST call `nginx.Apply(ctx, env, hostName, apexApp)` with the normalised host name (`host.NormalizeName` of the `host.name` store value) and the configured `host.apex` so routing is regenerated from the remaining manifests, report `nginx` with detail `<names> removed`, where `<names>` is `<app>.<host.name>`, followed by `, <host.name>` when the removed manifest was default, followed by `, <A>` when the removed `Manifest.App` equals `host.apex`, `<host.name>` being that normalised host name and `<A>` being the value `host.Apex` returns for it; then call `backup.Regenerate` and restart `litestream.service` exactly when it returns changed, finally reporting `litestream` with detail `<database.path> removed` when the removed manifest declared a database and regeneration changed, `updated` for another configuration change, or `unchanged` otherwise.
- R-X5J8-TEIS: After grammar and app-name validation, the `host.name not set` check, and the `host.apex` read, and before invoking `apps.Uninstall`, the uninstall CLI MUST, when `host.apex` is nonempty, call `host.Apex` with the normalised host name (`host.NormalizeName` of the `host.name` store value) and, on error, write exactly `opsctl: host.apex is set but host.name '<host.name>' has no parent domain` followed by LF to stderr, where `<host.name>` is that normalised host name, leave stdout empty with no stage report, exit 1, and perform no external command or host-state change; the Configure callback therefore receives only values `host.Apex` accepted.
- R-M6Y0-I4V2: Uninstall orchestration MUST finish stopping the app before restarting Litestream, so its existing replication process can perform its shutdown synchronization before the new configuration omits the removed database; it MUST preserve the database, WAL, SHM, and Litestream metadata for that synchronization, and on a successful shutdown synchronization the replica MUST contain the app's last committed transaction. This ordering MUST leave remaining databases configured and replicating after the restart.
- R-VLO7-YADY: Successful uninstall of an app with an existing state directory MUST leave that retained state-only service discoverable, causing status to report `<app> - - - -` when no binary, unit, or database declaration remains, and allowing backup to include all of its state under the manifest-free D11 rules; when no state directory existed, uninstall MUST not create one, and a remaining app directory with neither etc nor state MUST not be discoverable under D08. Uninstall MUST take the app's own name and, for a default, the host name out of routing; for the app `host.apex` names it MUST leave `host.apex` untouched, so the apex name moves to the 404 block under D06's rendering and answers from there until the app is installed again; and it MUST not change another app's files or unit or mutate remote parameters or deployment artifacts.
- R-ETFY-8BTG: Uninstall MUST emit exactly one outcome for each attempted stage in the order stop, unit, files, nginx, litestream through Report: successful effects use success true and render `<step>: ok (<detail>)` followed by LF to stdout; failed effects use success false and render `<step>: failed: <detail>` followed by LF to stdout, where detail explains the failure with CR and LF replaced by literal `\r` and `\n` respectively. Any action or report-write failure MUST prevent later stages, retain completed effects without rollback except nginx's own rollback, and return an operational error; unattempted stages MUST emit nothing, and a failed report callback MUST not be retried or cause a second outcome. Successful completion MUST exit 0 with empty stderr.
- R-XU5L-75BS: Only after the installed-app prerequisites of R-VGSM-F7F6 succeed MUST `Restart` determine through `Disabled` whether the app is disabled; for a disabled app it MUST execute no command that starts, restarts, or enables a unit and MUST succeed with a `ServiceReport` naming the app, its installed binary's `--version` response with the trailing line ending removed, and state `disabled`. Otherwise it MUST execute `systemctl restart ikigenba-<app>.service` whether the service unit is active, inactive, or failed, never restarting `ikigenba-<app>.socket` (a stopped socket is started by the service's `Requires=`), and query the service's resulting systemd state; only active completion MUST succeed, returning a `ServiceReport` naming the app, its installed binary's `--version` response with the trailing line ending removed, and state `active`.
- R-MBTM-17TU: Restart MUST neither write app installation or configuration files nor enable or disable any unit, reload nginx, control Litestream, access remote secrets, or alter another app; the new app process MUST use the unchanged installed EnvironmentFile and unit, while app-owned writes made by that process remain outside opsctl's no-write guarantee.
- R-VO40-PTVC: For an attempted Restart action, the CLI MUST emit exactly one service outcome on stdout: `service: ok (<app> <version> <state>)` followed by LF on success, using the returned `ServiceReport` fields so the state is `active` or `disabled`, or `service: failed: <reason>` followed by LF on failure, where reason is the LifecycleError.Message with CR and LF replaced by literal `\r` and `\n` respectively. Success MUST exit 0 with empty stderr; version MUST come from the installed executable, never an archive name, manifest, cached record, or remote deployment record. Failure to write either outcome MUST return an operational failure without retrying the report or emitting a second outcome.
- R-ME9E-SRB8: When restart fails or the resulting unit is not active, `Restart` MUST return a `LifecycleError` with Code 1 and Message `<app>: service failed to start`, obtain that unit's journal through `env.Execute` using `journalctl --unit ikigenba-<app>.service --no-pager --lines 50`, and preserve its output for CLI detail after one blank line with each line prefixed `> `; a failed journal query MUST preserve the original restart failure and identify the journal failure. No success line or rollback MUST occur, and a resulting failed unit MUST remain visible as `failed` to status.
- R-MFHB-6J1X: `Status` MUST use `Discover` to return exactly one row per discovered service in ascending bytewise name order, with each row derived independently from that service's current installed executable, systemd unit, and manifest-declared database; it MUST return an empty successful result when no service exists, including when `/opt` is absent.
- R-VPBX-3LM1: For each status row, `Status` MUST obtain Version by invoking `/opt/<name>/bin/<name> --version` through `env.Execute` and removing its trailing line ending; State from `systemctl show --property=LoadState --property=ActiveState ikigenba-<name>.service`; Socket from `systemctl show --property=LoadState --property=ActiveState --property=UnitFileState ikigenba-<name>.socket`, reading `disabled` when its `LoadState` is `loaded` and its `UnitFileState` is `disabled`, whatever its `ActiveState`, and otherwise its `ActiveState`; and JournalMode by reading the manifest-declared database mode without setting it. The two systemd queries MUST occur only when `ValidateName(name)` succeeds: when it fails, State and Socket MUST be `-` and no systemd query for a unit derived from that name may occur, while independently queryable Version and JournalMode remain reportable. Absent or unqueryable fields MUST independently be `-`, an unloaded unit MUST give `-`, and readable active, inactive, failed, disabled, wal, and delete facts MUST be retained rather than treated as command failures.
- R-MHX3-Y2JB: Status MUST perform no writes to configuration, units, database files, sidecars or other service data, no journal-mode assignment, no service control, and no cloud access; a manifest error MUST make the journal-mode field `-` without suppressing independently queryable version or unit state. The process queries MUST use D01's injected execution and root boundary.
- R-VQJT-HDCQ: On successful `Status`, the CLI MUST print each row as exactly `<Name> <Version> <State> <Socket> <JournalMode>` followed by LF, with no header, alignment padding, blank rows or diagnostic copies; all per-service findings, including failed units, non-WAL modes and unavailable fields, MUST exit 0 with empty stderr, while failure to enumerate services or write the report MUST exit 1 and write a D02 diagnostic explaining the command failure.
- R-VRRP-V53F: Lifecycle operational errors MUST use LifecycleError Code 1 with a concise Message and preserve underlying errors in Cause, including host.CommandError for external-process failures. For an attempted uninstall, restart, disable, or enable workflow, the CLI MUST retain outcome reports already written and emit `opsctl: uninstall failed`, `opsctl: restart failed`, `opsctl: disable failed`, or `opsctl: enable failed`, respectively, followed by LF on stderr, without copying a report or repeating its failure reason; D02 quoted external-process detail and the specified startup journal MUST remain available after one blank line. Report-write failures MUST use Code 1 and MUST not cause duplicate report or reason rendering. Before a workflow begins, failures MUST instead emit Message prefixed by `opsctl: ` and LF with no outcome report. The CLI MUST return Code and never write usage text to stderr.
- R-PWID-NYOW: The status JournalMode field MUST report the persistent database mode observable by a fresh read-only SQLite connection: `wal` for a valid SQLite file in WAL format and `delete` for a valid SQLite file in rollback format, with `-` for missing, unreadable, malformed or unsupported files; connection-local choices such as truncate, persist, memory and off MUST not be inferred from another process's connection, and obtaining this field MUST require no additional Go module or installed host executable.

- R-VSZM-8WU4: Uninstall, restart, disable, and enable workflows MUST begin only after grammar, app-name validation, and required hook and configuration validation succeed; failures of those preliminary checks MUST produce diagnostics only. Uninstall owns stop, unit and files outcomes, while its CLI Configure callback owns nginx and litestream outcomes; a Configure failure MUST propagate without another outcome from Uninstall. The stop stage MUST include checking app-directory, binary, manifest and unit prerequisites, observing current unit state, stopping when required and disabling; the remaining stages encompass their existing named effects. Restart's service stage MUST include installed-app prerequisite checks, restarting the unit, observing its resulting state and obtaining its version. On an action failure, the owning stage MUST report failure before returning; error details MUST survive through LifecycleError.Cause, including both action and report callback errors when both occur.

- R-VU7I-MOKT: `Disabled` MUST reject a name that `ValidateName` rejects with an error before executing anything; otherwise it MUST execute exactly `systemctl show --property=LoadState --property=UnitFileState ikigenba-<app>.socket` through `env.Execute` and return true exactly when the output holds the lines `LoadState=loaded` and `UnitFileState=disabled`, and false for any other file state and for a unit that is not loaded, such as an app with no socket unit; an execution error or nonzero exit MUST return an error wrapping `*host.CommandError`. It MUST change nothing. An app is *disabled* exactly when `Disabled` reports true; nothing else records the state, and D06, D09, D13 and D14 use this predicate.
- R-VVFF-0GBI: `Disable` MUST, after name validation and before any other host access, refuse the app name `auth` by reporting `stop` failed with detail `auth is the authenticator and cannot be disabled` and returning a `LifecycleError` with Code 1 and that Message, whether or not `auth` is installed and whatever else is on the host, executing no command, invoking no Configure callback, and changing nothing. For every other name, `Disable` and `Enable` MUST require non-nil hooks, `/opt/<app>/`, `bin/<app>`, a readable valid installed manifest matching the app, and both `/etc/systemd/system/ikigenba-<app>.socket` and `/etc/systemd/system/ikigenba-<app>.service`, reporting a missing unit file as `<app> is not installed`; a prerequisite failure MUST be reported as the failed first-stage outcome (`stop` for `Disable`, `enable` for `Enable`) and change nothing.
- R-XVDH-KX2H: `Disable`'s `stop` stage MUST observe both units' active and unit-file states through `systemctl show --property=ActiveState --property=UnitFileState <unit>` executed once per unit via `env.Execute`, a unit counting as active exactly when `ActiveState=active`, as disabled exactly when `UnitFileState=disabled`, and as enabled exactly when `UnitFileState=enabled`; when both units are disabled and neither is active it MUST execute no stop or disable command and report `stop` with detail `ikigenba-<app>.socket, ikigenba-<app>.service already inactive, disabled`; otherwise it MUST execute `systemctl stop ikigenba-<app>.socket`, then `systemctl stop ikigenba-<app>.service`, then `systemctl disable ikigenba-<app>.socket ikigenba-<app>.service`, and only after all succeed report `stop` with detail `ikigenba-<app>.socket, ikigenba-<app>.service stopped, disabled` when either unit was active at observation or `ikigenba-<app>.socket, ikigenba-<app>.service already inactive, disabled` otherwise. `Disable` MUST NOT change either unit file or anything under `/opt/<app>/`.
- R-XWLD-YOT6: `Enable`'s `enable` stage MUST observe both units' active and unit-file states through `systemctl show --property=ActiveState --property=UnitFileState <unit>` executed once per unit via `env.Execute`, a unit counting as active exactly when `ActiveState=active`, as disabled exactly when `UnitFileState=disabled`, and as enabled exactly when `UnitFileState=enabled`; when both units are already enabled it MUST execute no enable command, and otherwise it MUST execute `systemctl enable ikigenba-<app>.socket ikigenba-<app>.service`; it MUST then execute `systemctl start ikigenba-<app>.socket` when the socket is not active, and only after all succeed report `enable` with detail `ikigenba-<app>.socket, ikigenba-<app>.service already enabled` when no enable command was needed or `ikigenba-<app>.socket, ikigenba-<app>.service` otherwise. `Enable` MUST NOT change either unit file or anything under `/opt/<app>/`.
- R-VZ34-5RJL: After a successful `stop` stage `Disable`, and after a successful `enable` stage `Enable`, MUST invoke `hooks.Configure` exactly once with the installed manifest; the CLI-supplied callback MUST call `nginx.Update(ctx, env, hostName, apexApp)` (D06) with the normalised host name and the `host.apex` value the CLI read, and report `nginx` only after it succeeds, with detail `unchanged` when `Update` returned false and otherwise the routed names followed by ` disabled` for `Disable` or the routed names alone for `Enable`, the routed names being `<app>.<host.name>`, followed by `, <host.name>` when the manifest is default, followed by `, <A>` when `Manifest.App` equals `host.apex`, with `<A>` the value `host.Apex` returns for the normalised host name.
- R-W0B0-JJAA: After a successful Configure callback, `Enable`'s `service` stage MUST execute `systemctl start ikigenba-<app>.service` when the service is not active, then determine the service's resulting systemd state; only `active` MUST succeed, reporting `service` with detail `<app> <version> active`, the version being the installed binary's `--version` response with its trailing line ending removed. On failure or a non-active result it MUST fail with Message `<app>: service failed to start`, obtain the journal through `env.Execute` using `journalctl --unit ikigenba-<app>.service --no-pager --lines 50` and preserve its output for CLI detail after one blank line with each line prefixed `> `, leave both units enabled and nginx as the callback left it, and roll nothing back.
- R-W1IW-XB0Z: `Disable` MUST emit exactly one outcome for each attempted stage in the order `stop`, `nginx`, and `Enable` in the order `enable`, `nginx`, `service`, through `Report`: success renders `<step>: ok (<detail>)` followed by LF to stdout and failure renders `<step>: failed: <detail>` followed by LF to stdout, with CR and LF in detail replaced by literal `\r` and `\n`. Any action or report-write failure MUST prevent later stages and retain completed effects without rollback except `nginx.Update`'s own; unattempted stages MUST emit nothing, and a failed report callback MUST not be retried or cause a second outcome. `Disable` and `Enable` own their `stop`, `enable`, and `service` outcomes; the CLI Configure callback owns the `nginx` outcome, and a Configure failure MUST propagate without another outcome. Successful completion MUST exit 0 with empty stderr.
- R-W3YP-OUID: For `disable` and `enable` with any app other than `auth` given to `disable`, after grammar and app-name validation and before invoking `apps.Disable` or `apps.Enable`, the CLI MUST read `host.name` and `host.apex` from the `config.Store` rooted at `Deps.Root`, taking `host.NormalizeName` of `host.name` as the host name, reporting `host.name not set` when it is absent or empty after normalisation, treating an unset `host.apex` as the empty string and failing with a D02 diagnostic on any other read error; when `host.apex` is nonempty and `host.Apex` refuses the normalised host name it MUST write exactly `opsctl: host.apex is set but host.name '<host.name>' has no parent domain` followed by LF to stderr with that normalised name. Each of these failures MUST leave stdout empty with no outcome line, exit 1, and execute no command or change host state. For `disable auth` the CLI MUST invoke `apps.Disable` without reading configuration, so the refusal holds whatever the store holds.
- R-W56M-2M92: Disable and enable MUST each be idempotent with respect to the state they leave: disabling an app whose units are both disabled and inactive and enabling an app whose units are both enabled, with its socket listening and its service active, MUST enable, disable, start, or stop no unit, publish no nginx configuration and reload nothing when the rendered configuration equals the file in place, and exit 0. Neither command MAY change another app's units or files, the store, or anything under `/opt/<app>/`, other than systemd's enablement links and the nginx configuration.
