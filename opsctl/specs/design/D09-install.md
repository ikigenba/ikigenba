# D09-install

Installation turns an S3 artifact and host-local configuration into a running app.
The CLI composes routing and replication between installation and app startup.
Every attempted artifact download reports its success or failure. Installation
and restart report service completion; status separately reports host facts.
App-owned persistent data survives installation. The installed trees belong to
root and the service account reaches them through its group, so the app can
run and read its configuration but cannot write `bin/`, `etc/`, or `share/` or
the files in them, while `/opt/<app>` itself stays writable by the account so
the app can create `state/` and `cache/`.

Every app runs as two units. `ikigenba-<app>.socket` holds the app's Unix
socket, `/run/ikigenba/<app>.sock`, owned by the `ikigenba` user and the
`nginx` group with mode `0660`, so nginx and the platform's own processes can
reach it and nothing else can; systemd creates `/run/ikigenba/` for it
(systemd.socket(5), `DirectoryMode=`). `ikigenba-<app>.service` is a
`Type=notify` service handed that socket, which it requires and follows. Both
are enabled, so the app starts at boot. An install over a running app
restarts only the service: the socket keeps listening, so requests wait on it
and are answered by the new process. An app disabled with `opsctl disable`
(D10) stays disabled: its files and units are replaced, but nothing is enabled
or started. The drain deadline and stop timeout come from the store (D08):
`DRAIN_SECONDS` in `etc/env`, `TimeoutStopSec=` in the service unit; `init`'s
`apps` step writes changed values into every installed app.

Secret values come from the parameter `/<host.name>/<app>`, never from the
artifact. The nginx line names every name the app answers at: its own, the
host's when it is the default, and the root domain's apex when `host.apex`
names it. Install reads `host.apex` from the store before the workflow begins,
refuses there when the host name has no parent domain, and otherwise passes it
through to nginx; it never sets or changes it. The host name every install
surface uses (the secrets parameter, the nginx call, the report detail, and the
apex check) is `host.NormalizeName` of the stored `host.name` (D01).

## REQUIREMENTS

- R-OJB9-VRYL: Package `internal/apps` MUST own artifact installation and app unit publication, depending only on `internal/config`, `internal/host`, and `internal/cloud` within this module; `internal/cli` MUST own install grammar, rendering of reports and diagnostics, and composition with `internal/nginx` and `internal/backup`.
- R-YNIK-9QOD: Package `internal/apps` MUST export `InstallHooks` as a struct with exactly `Report func(step, detail string, success bool) error` and `Configure func(context.Context, Manifest) error` fields.
- R-OLR2-NBFZ: Package `internal/apps` MUST export `Install(ctx context.Context, env host.Env, remote cloud.Env, store config.Store, uri string, hooks InstallHooks) error`.
- R-OMYZ-136O: Package `internal/apps` MUST export `InstallError` as a struct with exactly `Code int`, `Message string`, and `Cause error` fields; `(*InstallError).Error() string` MUST return `Message`, and `(*InstallError).Unwrap() error` MUST return `Cause`.
- R-UEZQ-KBFN: `opsctl install --help` and `opsctl install -h` MUST, for any effective user id, leave host state unchanged, perform no host-file access, command execution or cloud call, exit 0 with empty stderr, and write exactly `"Usage: opsctl install URI\n\nInstall the app at URI, an s3:// object holding an <app>-<tag>.tar.xz built by\ndevctl. The app name and the secrets it needs are read from\netc/manifest.toml inside it; the secret values are read from the parameter\n/<host.name>/<app>.\n\nNothing under /opt/<app>/state/ or /opt/<app>/cache/ is touched, so installing\nover a running app keeps its data. Safe to re-run. An app that is disabled\nstays disabled: its files and units are replaced, but neither unit is enabled\nor started until 'opsctl enable'.\n\nThe nginx configuration and /etc/litestream.yml are regenerated from every app\non the host, so an app that declares a [database] is replicated from the\nmoment it is installed. litestream.service is restarted only when its\nconfiguration changed.\n\nConfiguration keys:\n  aws.region          the region this host's parameters and artifacts live in\n  host.name           the fully-qualified name this host answers at\n  apps.drain_seconds  how long the app may drain when stopped (default 5)\n  apps.stop_seconds   how long systemd waits for it to stop (default 10)\n"` to stdout, where `\n` denotes one LF byte.
- R-OPER-SMO2: The install action MUST accept exactly one absolute `s3://` URI with a nonempty bucket and object key and no userinfo, port, opaque form, query or fragment; before any host access, missing operands, multiple operands, and an invalid URI MUST respectively exit 2 with empty stdout and the three LF-terminated stderr lines `opsctl: install needs URI`, an empty line, `see 'opsctl install --help' for usage`, substituting `opsctl: install takes one URI` or `opsctl: install takes an s3:// URI` for the first line in the latter two cases.
- R-WWZY-50BX: `Install` MUST read `host.name` from `store` and use `host.NormalizeName` of that value as the host name; it MUST reject invalid URI input, nil `hooks.Report` or `hooks.Configure`, missing or empty `aws.region`, or a `host.name` that is missing or empty after normalisation, before fetching or mutation; missing configuration MUST identify `<key> not set`, so a `host.name` empty after normalisation is `host.name not set`; for valid configuration it MUST fetch the supplied object through `remote.Open` using `aws.region` and `Client.GetObject`, using the host cloud credentials and never receiving artifact bytes from command stdin or fetching secret values from the artifact.
- R-UG7M-Y36C: `Install` MUST validate the complete xz-compressed tar input before changing installed host state: it MUST require `etc/manifest.toml` parsed through `ParseManifest`, a nonempty `Manifest.App` accepted by `ValidateName`, and an executable regular `bin/<app>`; it MUST accept optional `share/` and additional files under `bin/` and `etc/`, reject content outside those three top-level directories, duplicate archive paths, links, special files, absolute paths and `.` or `..` path components, and never follow an existing destination symlink to write outside the intended installed tree.
- R-OUAD-BPMU: Before changing installed host state, `Install` MUST use `Discover` to inspect every service manifest and reject discovery or per-service manifest failures; when the incoming manifest is default, it MUST reject any different service whose manifest is default with `<app>: <other> is already the default app`, while permitting replacement of the same default app and permitting a host with no default.
- R-WY7U-IS2M: After artifact and default validation but before mutation, `Install` MUST obtain every distinct name in `Manifest.Secrets` from the parameter `/<host.name>/<app>`, where `<host.name>` is the normalised host name (`host.NormalizeName` of the `host.name` store value), through `Client.ReadSecrets`; it MUST skip the parameter read for an empty secret list, omit unrequested parameter keys, and treat a missing parameter as containing no keys, reporting the first missing name in manifest order as `<app>: no value for '<name>' in /<host.name>/<app>` with that same normalised host name; an empty present value MUST count as present.
- R-UINF-PMNQ: `Install` MUST validate that secret and plain-setting names match `[A-Za-z_][A-Za-z0-9_]*`, that neither source defines `DRAIN_SECONDS`, that the two sources have no overlapping names, and that values contain no NUL, CR or LF, rejecting violations before mutation without printing their values; the installed `etc/env` MUST have mode `0600` and hold one LF-terminated `NAME=value` entry per requested secret and plain setting plus `DRAIN_SECONDS=<n>`, where `<n>` is the `Timeouts.DrainSeconds` value `ReadTimeouts` (D08) returned, in decimal without leading zeros; values MUST be encoded for systemd EnvironmentFile syntax to preserve their literal value, and the file MUST hold no `PORT` entry and no other parameter keys.
- R-OXY2-H0UX: After all artifact, default and secret validation succeeds, `Install` MUST replace `/opt/<app>/bin/`, `/opt/<app>/etc/`, and `/opt/<app>/share/` with the artifact contents, removing stale installed contents including an old share directory when the new archive omits it, and publish the generated `etc/env`; it MUST neither create nor touch `/opt/<app>/state/` or `/opt/<app>/cache/`, modify any other service's files or unit, run a schema migration, create a database, or load seed data.
- R-EPO4-BRY5: When `/opt` is absent under `env.Root`, `Install` MUST create it directly under `env.Root` owned by root with mode `0755`; an existing `/opt` MUST be left unchanged in ownership and mode.
- R-UJVC-3EEF: `Install` MUST publish `/etc/systemd/system/ikigenba-<app>.socket` with exactly the bytes `"[Unit]\nDescription=Ikigenba <app> socket\n\n[Socket]\nListenStream={S}\nSocketUser=ikigenba\nSocketGroup=nginx\nSocketMode=0660\nRemoveOnStop=yes\nBacklog=4096\n\n[Install]\nWantedBy=sockets.target\n"` and `/etc/systemd/system/ikigenba-<app>.service` with exactly the bytes `"[Unit]\nDescription=Ikigenba <app> app\nRequires=ikigenba-<app>.socket\nAfter=ikigenba-<app>.socket\n\n[Service]\nType=notify\nExecStart={O}/bin/<app>\nWorkingDirectory={O}\nEnvironmentFile={O}/etc/env\nUser=ikigenba\nRestart=on-failure\nTimeoutStopSec={T}\n\n[Install]\nWantedBy=multi-user.target\n"`, where `\n` denotes one LF byte, `<app>` is the app name, `{S}` is `/run/ikigenba/<app>.sock` and `{O}` is `/opt/<app>`, each resolved under `env.Root` by D01's root boundary, and `{T}` is the `Timeouts.StopSeconds` value `ReadTimeouts` (D08) returned, in decimal without leading zeros; every install MUST replace both files, including an install over an installed or disabled app.
- R-UL38-H654: In the unit stage `Install` MUST ensure a non-root system account named `ikigenba` exists without a login shell or creating a home directory and that a group named `ikigenba` is that account's primary group: when the account is absent it MUST create it by executing `useradd --system --no-create-home --shell /usr/sbin/nologin --user-group ikigenba` through `env.Execute`, and when the account exists it MUST determine the account's primary group name by executing `id --group --name ikigenba` through `env.Execute` and fail the unit stage, preserving the underlying error in `InstallError.Cause`, when that name is not `ikigenba`. All host paths MUST follow D01's root boundary. The `/opt/<app>` directory MUST be writable by that account so the app can create its own state and cache at startup, without creating or changing either directory during installation.
- R-UMB4-UXVT: After publishing both unit files, `Install` MUST execute `systemctl daemon-reload`; then, unless the app was disabled at invocation (`Disabled`, D10), it MUST execute `systemctl enable ikigenba-<app>.service` followed by `systemctl enable --now ikigenba-<app>.socket`, so both units are enabled and the socket is listening before the unit stage reports success and before the Configure callback regenerates nginx. For an app disabled at invocation it MUST execute neither command nor any other command that enables or starts either unit, so both remain disabled and inactive. No install MAY stop or restart the socket unit.
- R-EM0F-6GQ2: After every successful install, including one over an already installed app, every directory at or under `/opt/<app>/bin/`, `/opt/<app>/etc/`, and `/opt/<app>/share/` when present MUST be owned by user `root` and group `ikigenba` with mode `0750`, and every regular file beneath them MUST be owned by user `root` and group `ikigenba` with mode `0750` when its archived mode carried any execute bit and `0640` otherwise, except `etc/env`, which keeps mode `0600`; establishing that shape MUST NOT change anything under `/opt/<app>/state/` or `/opt/<app>/cache/`, and `/opt/<app>` itself MUST keep the ownership by the `ikigenba` account and the writability the unit stage gives it.
- R-EN8B-K8GR: In the unit stage, after account preparation succeeds and before the successful unit report, `Install` MUST apply installed-tree ownership through exactly one coreutils `chown --recursive root:ikigenba` invocation executed through `env.Execute`, naming exactly the existing trees `/opt/<app>/bin`, `/opt/<app>/etc`, and `/opt/<app>/share` in that order, each resolved under `env.Root`, and naming no path under `/opt/<app>/state/` or `/opt/<app>/cache/`; it MUST apply the installed-tree directory and file modes directly under `env.Root` so a test observes them with `Lstat`.
- R-EOG7-Y07G: A failure applying installed-tree ownership or modes MUST be the unit stage's failed outcome with the underlying error preserved in `InstallError.Cause`, and `Install` MUST emit no successful unit report after such a failure.
- R-WZFQ-WJTB: After grammar validation and the root check and before invoking `apps.Install`, the install CLI MUST read `host.name` and `host.apex` from the `config.Store` rooted at `Deps.Root`, taking `host.NormalizeName` of the `host.name` value as the host name and treating an unset `host.apex` as the empty string; when both the normalised host name and `host.apex` are nonempty it MUST call `host.Apex` with the normalised host name and, on error, write exactly `opsctl: host.apex is set but host.name '<host.name>' has no parent domain` followed by LF to stderr, where `<host.name>` is the normalised host name, leave stdout empty with no stage report, exit 1, and perform no fetch, cloud call, external command, or host-state change. When `host.name` is unset or empty after normalisation this pre-check MUST NOT run, so `Install` reports `host.name not set` as it does without `host.apex`; a `host.apex` read failure other than unset MUST produce a D02 diagnostic and exit 1 before the workflow begins, with no stage report.
- R-XMU6-WIVM: `Install` MUST call `hooks.Configure` exactly once with the installed manifest after the unit stage succeeds and before starting or restarting the app; the CLI-supplied callback MUST call `nginx.Apply(ctx, env, hostName, apexApp)` with the normalised host name (`host.NormalizeName` of the `host.name` store value) and the `host.apex` value the CLI read before the workflow began, then determine through `Disabled` (D10) whether the app is disabled, report nginx completion only after both succeed, with the detail R-UOQX-MHD7 specifies, then call `backup.Regenerate`, execute `systemctl restart litestream.service` exactly when regeneration returned changed, and report Litestream completion only after all required actions succeed; any failure MUST prevent later actions, including app start. `Install` and its CLI callback MUST NOT set, clear, or change `host.apex`: which app holds the apex is read from the store and never decided by installation.
- R-UNJ1-8PMI: After successful configuration, for an app disabled at invocation `Install` MUST NOT start or restart either unit and MUST report service success with state `disabled`; otherwise it MUST execute `systemctl restart ikigenba-<app>.service` when that service unit was active at invocation and `systemctl start ikigenba-<app>.service` otherwise, determine the resulting service state from systemd, and report success only for `active`. In either case the installed binary invoked with `--version` MUST supply the reported version, with its trailing line ending removed, without inferring it from the URI, archive name, manifest or a remote record.
- R-UOQX-MHD7: `Install` MUST send successful reports through `hooks.Report` only after the corresponding result holds, in the order `file`, `secrets`, `unpack`, `unit`, then the CLI callback's `nginx` and `litestream` reports, then `service`; details MUST be `<app>` with `, default` appended for a default app, `<count> keys` counting distinct requested secrets, `/opt/<app>`, `ikigenba-<app>.socket, ikigenba-<app>.service`, the routed names followed by ` disabled` when the app was disabled at invocation (the CLI callback observes this through `Disabled`, D10, which the unit stage leaves unchanged), the database's service-relative path when replication configuration changed or `unchanged` when it did not, and `<app> <version> active`, or `<app> <version> disabled` for an app disabled at invocation, respectively. The routed names MUST be `<app>.<host.name>`, followed by `, <host.name>` when the manifest is default, followed by `, <A>` when `Manifest.App` equals the configured `host.apex`, where `<host.name>` is the normalised host name (`host.NormalizeName` of the `host.name` store value) and `<A>` is the value `host.Apex` returns for that normalised host name, so an app that is both default and apex reports all three names in that order and an app `host.apex` does not name reports no apex name. When configuration changes after removing the incoming database, the Litestream detail MUST be `updated`.
- R-UPYU-093W: The file stage of `Install` MUST include complete artifact validation, discovery of installed manifests, competing-default validation, and the initial observation of the app's units needed to select start or restart and to determine whether the app is disabled (`Disabled`, D10); only after all succeed MUST it emit the successful file report, before obtaining required secrets. A failure in those checks MUST emit one failed file outcome and prevent the secrets stage and installed-state mutation. Failure of either file report callback MUST stop installation without retrying the report.
- R-XQHW-1U3P: Throughout installation, the install operation and its CLI composition MUST NOT intentionally serialize secret or plain-setting environment values into reports, stdout, stderr diagnostics, or subprocess arguments on success or failure; key names, counts and separately specified manifest metadata such as the app name MUST remain reportable in their declared formats. This constraint governs emitting environment values as such, not coincidental matching bytes in metadata or relaying externally produced diagnostics. External journal output MUST be preserved without redaction or suppression even when it contains environment values, with each line quoted after one blank line according to the diagnostic convention.
- R-EKWN-JXML: The fetch stage of `Install` MUST begin with `remote.Open` after valid command, hook, and configuration inputs, and report exactly one outcome through `hooks.Report` before artifact validation: success true only after GetObject returned a reader, the complete object was read, and the reader closed successfully, with detail `<object-basename>, <size> MiB`, size being the received byte count divided by 1048576 rounded to one decimal place; success false for Open, credential, GetObject, missing-object, incomplete-read, read, or close failures, with detail explaining the failure and `<object-basename>: no such object` for a missing object. Failed fetch MUST prevent validation, secret reads, and installed-state mutation. Download errors MUST remain available through InstallError.Cause; a simultaneous report callback error MUST preserve both errors without another fetch report. Rejected grammar, invalid hooks, or configuration failures before the fetch stage MUST produce no fetch report.
- R-XLMA-IR4X: For every attempted install stage, Install or its Configure callback MUST invoke its Report callback exactly once with that stage's outcome: success true renders exactly `<step>: ok (<detail>)` followed by LF to stdout, and success false renders exactly `<step>: failed: <detail>` followed by LF to stdout, where failed detail explains the stage failure with CR and LF replaced by literal `\r` and `\n` respectively. Unattempted later stages MUST emit nothing. Successful installation MUST exit 0 with empty stderr and end with `service: ok (<app> <version> active)` followed by LF, or `service: ok (<app> <version> disabled)` followed by LF for an app disabled at invocation, using the same three-value service format as restart; status retains its independent five-column D10 report. A report-write failure MUST fail the operation with Code 1, prevent later actions, and never retry or emit a second outcome for that stage.
- R-XO23-AAMB: After its fetch failure report succeeds, `Install` MUST return an `InstallError` with Message `artifact download failed` and Code 2 for a missing object or Code 1 for any other fetch failure. Missing manifests MUST produce Code 2 with failure reason `<object-basename>: no etc/manifest.toml in the file`; a `ParseManifest` rejection of the `app` value by `ValidateName`, and an empty or absent `app`, MUST produce Code 2 with reason `'<name>' is not a usable app name`; every other `ParseManifest` error, including malformed TOML and the D08 `port` refusal, MUST produce Code 2 with reason `<object-basename>: etc/manifest.toml: <decoder complaint>`, the complaint being that error's text; these artifact failures MUST emit a failed file outcome, leave installed host state unchanged, and emit no successful file or later-stage report. A fetch-report callback failure MUST instead return Code 1 while preserving any fetch failure through Cause.
- R-EOKC-P8UO: `Install` MUST return invalid archive/layout/manifest/URI failures as `InstallError` with Code 2 and operational failures, including configuration reads, cloud access other than missing objects, missing secrets, competing default, subprocess failures, callback failures and filesystem failures, with Code 1. For failure of an attempted workflow stage, the CLI MUST retain reports already written and write `opsctl: artifact download failed` followed by LF for a fetch failure or `opsctl: install failed` followed by LF for another stage failure; it MUST not copy the failed report or repeat its reason on stderr. External process failures MUST preserve host.CommandError in Cause and retain D02 quoted external detail, including the separately specified startup journal; cloud and report-write causes MUST not be automatically rendered as duplicate diagnostic reasons. A failure before the workflow begins MUST instead emit Message prefixed by `opsctl: ` and LF with no stage report. The CLI MUST return Code.
- R-P8X5-WYJ6: When app start or restart fails, or the resulting unit is not active, `Install` MUST fail with Message `<app>: service failed to start`, obtain that unit's journal through `env.Execute` using `journalctl --unit ikigenba-<app>.service --no-pager --lines 50`, and preserve its output for CLI detail after one blank line with each line prefixed `> `; failure to obtain the journal MUST retain the original start failure and identify the journal failure without printing a successful service report.
- R-XP9Z-O2D0: A failure after installed files have been changed MUST stop subsequent work and retain completed installation effects without rollback, except for the rollback that `nginx.Apply` itself promises; a failed app MUST therefore retain its unpacked files, both units with the enablement the unit stage left them in, and completed nginx and replication changes, and successful reinstallation of the same artifact MUST repeat the install reports and succeed while preserving state and cache and leaving byte-identical Litestream configuration and its unit untouched.

- R-USEM-RSLA: Install MUST attempt the existing stages in order fetch, file, secrets, unpack, unit, nginx, litestream, service, stopping at the first action or report failure. Install owns fetch, file, secrets, unpack, unit and service outcomes; the CLI Configure callback owns nginx and litestream outcomes and MUST propagate failure without Install inventing an additional stage outcome. The secrets stage includes parameter retrieval and environment-name/value validation; unpack includes installed-directory replacement and environment publication; unit includes account preparation, installed-tree ownership and mode application, publication of both unit files, daemon reload, and, for an app not disabled at invocation, enablement of both units and starting the socket; nginx includes nginx.Apply; litestream includes backup.Regenerate and any required restart; service includes app start or restart and resulting-state confirmation for an app not disabled at invocation, and binary-version reporting for every app. Each failed stage MUST supply its failure reason to Report and preserve its underlying error in InstallError.Cause; a report callback error MUST stop work and preserve both errors when the stage also failed.

- R-UTMJ-5KBZ: `Install` MUST obtain the app timing settings through `ReadTimeouts` (D08) from `store` after its URI, hook, `aws.region`, and `host.name` validation and before `remote.Open`; on error it MUST return an `InstallError` with Code 1 and Message equal to that error's text before any fetch, report, cloud call, external command, or host-state change, so the CLI writes `opsctl: <message>` followed by LF to stderr with empty stdout and no stage report, and exits 1.

- R-UUUF-JC2O: Package `internal/apps` MUST export `SetupTimeouts(ctx context.Context, env host.Env, store config.Store) error`.
- R-UW2B-X3TD: `SetupTimeouts` MUST obtain the timing settings through `ReadTimeouts` and return its error before any write. It MUST treat as installed apps exactly the services `Discover` returns whose name `ValidateName` accepts and that hold a regular file `/opt/<name>/bin/<name>`, and for each MUST set the `DRAIN_SECONDS` entry of `/opt/<name>/etc/env` to the drain value in decimal without leading zeros, replacing an existing `DRAIN_SECONDS` entry in place or appending one when absent and leaving every other line byte-identical with mode `0600`, and MUST make `/etc/systemd/system/ikigenba-<name>.service` hold the service-unit bytes install publishes for that name and the stop value; an app's settings changed exactly when either file's bytes differ from their previous bytes, and a file whose bytes would not change MUST NOT be rewritten. A missing or unreadable `etc/env` of an installed app MUST return an error identifying the app. Services that are not installed apps MUST NOT be written, and no unit may be created for them.
- R-Y1GZ-HRRY: After its writes `SetupTimeouts` MUST execute `systemctl daemon-reload` exactly when some service unit file changed, then, in ascending name order, `systemctl restart ikigenba-<name>.service` for every installed app whose settings changed, which `Disabled` (D10) does not report disabled, and whose `ikigenba-<name>.socket` is active, so every running app, idle service included, runs with the new values, while an app whose socket an operator stopped is not started and takes them at its next start; it MUST NOT start, restart, enable, or disable either unit of a disabled app or of an app whose settings did not change, and MUST NOT stop, start, or restart any socket unit, so a disabled app is rewritten and left disabled. A rerun with unchanged settings MUST write no file and execute no `systemctl` command other than the read-only `Disabled` query.
- R-UYI4-ONAR: `SetupTimeouts` MUST return discovery, configuration, filesystem, and unit-operation failures as errors, representing execution failures and nonzero process exits with `host.CommandError`, perform no later action after a failure, and preserve earlier effects without rollback; it MUST NOT change the store, anything under `/opt/<name>/state/` or `/opt/<name>/cache/`, any socket unit file, or any file other than the `etc/env` files and service unit files it is specified to write.
